---
phase: 02-income-expenses
plan: 03
type: execute
wave: 1
depends_on: ['02-01']
files_modified:
  - apps/api/src/schemas/expense.ts
  - apps/api/src/services/expense-service.ts
  - apps/api/src/routes/expenses.ts
  - apps/api/tests/expense.test.ts
  - apps/web/src/app/expenses/page.tsx
  - apps/web/src/app/expenses/new/page.tsx
  - apps/web/src/app/expenses/new/_actions/create-expense.ts
  - apps/web/src/app/expenses/_components/expenses-overview.tsx
  - apps/web/src/app/expenses/_components/expense-list.tsx
  - apps/web/src/app/expenses/_components/expense-form.tsx
  - apps/web/src/app/expenses/_components/edit-expense-dialog.tsx
  - apps/web/src/app/expenses/_components/expense-actions.ts
autonomous: false
requirements: [EXP-01, EXP-02, EXP-03, EXP-04]
must_haves:
  truths:
    - 'User can record an expense with category, amount, date, payment method, and description'
    - 'User can browse a paginated expense list filtered by date range with load-more'
    - 'User can edit an expense from a dialog opened on the list row'
    - 'User can soft-delete an expense (it is excluded from totals) and restore it'
  artifacts:
    - path: 'apps/api/src/services/expense-service.ts'
      provides: 'createExpenseService factory: list/create/getById/update/delete(soft)/restore'
      exports: ['createExpenseService']
    - path: 'apps/api/src/routes/expenses.ts'
      provides: 'GET / POST / GET /:id / PUT /:id / DELETE /:id / PATCH /:id/restore'
      contains: '/:id/restore'
    - path: 'apps/web/src/app/expenses/page.tsx'
      provides: 'SSR expense list page'
      min_lines: 10
    - path: 'apps/api/tests/expense.test.ts'
      provides: 'unit tests EXP-01..04 incl soft delete/restore'
      contains: 'restore'
  key_links:
    - from: 'apps/web/src/app/expenses/new/_actions/create-expense.ts'
      to: '/api/expenses'
      via: 'apiFetch POST with toCents conversion'
      pattern: 'toCents'
    - from: 'apps/api/src/services/expense-service.ts'
      to: 'expenses table'
      via: 'soft delete sets deletedAt; totals filter isNull(deletedAt)'
      pattern: 'deletedAt'
    - from: 'apps/web/src/app/expenses/_components/expense-actions.ts'
      to: 'PATCH /api/expenses/:id/restore'
      via: 'restoreExpenseAction'
      pattern: 'restore'
---

<objective>
Complete expense vertical slice: record an expense with payment method (EXP-01), browse a date-filtered load-more list (EXP-02), edit (EXP-03), and soft-delete + restore (EXP-04) — DB → API → UI end to end.

Purpose: Deliver the expense management capability of Phase 2 in parallel with the income slice (no shared files).
Output: expense Zod schemas (with D-10 validated payment methods), expense service (soft delete/restore), expense routes (incl. PATCH /:id/restore), unit tests, and the full expense UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-income-expenses/02-CONTEXT.md
@.planning/phases/02-income-expenses/02-RESEARCH.md
@.planning/phases/02-income-expenses/02-PATTERNS.md
@.planning/phases/02-income-expenses/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 02-01 (foundation) — use directly. -->

apps/web/src/server/api.ts: `apiFetch<T>(path, init?)`, `class ApiError { status; code }`.
apps/web/src/lib/format-currency.ts: `formatCurrency(cents)`, `toCents(pesos)`.
apps/web/src/lib/format-date.ts: `formatDate(iso)`.
apps/web/src/lib/constants.ts: `PAYMENT_METHODS` (5 {value,label}), `PaymentMethodValue`.
apps/api/src/schemas/common.ts: `paginationWithDateSchema`, `idParamSchema`.
apps/api/src/routes/expenses.ts: STUB exists — `expensesRouter` exported, mounted behind requireAuth at /api/expenses. Fill handlers here.
apps/api/tests/helpers/db.ts: `createTestDb()` — drizzle instance, 4 tables migrated.
packages/db schema: `expenses` (incl. `deletedAt`, nullable `paymentMethod`), `expenseCategories`.
requireAuth sets `c.get('userId')`.
</interfaces>

<reference_implementation>
PRIMARY fidelity source — read before implementing:

- /mnt/c/dev/profitfirst/practice/src/server/services/expense-service.ts (CRUD, soft delete, restore, resolveCategoryName)
- /mnt/c/dev/profitfirst/practice/src/server/routes/expenses.ts (route surface incl. PATCH /:id/restore)
- /mnt/c/dev/profitfirst/practice/src/server/validations/expense.ts
- /mnt/c/dev/profitfirst/practice/src/app/(dashboard)/expenses/ (page/dialog/filter structure)
- /mnt/c/dev/profitfirst/practice/src/lib/constants.ts PAYMENT_METHODS (lines 136-142)
  Adapt: businessId → userId; tighten paymentMethod to the 5-value enum (D-10).
  </reference_implementation>
  </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expense Zod schemas + service (soft delete/restore) + routes (API slice)</name>
  <files>apps/api/src/schemas/expense.ts, apps/api/src/services/expense-service.ts, apps/api/src/routes/expenses.ts, apps/api/tests/expense.test.ts</files>
  <read_first>
    - /mnt/c/dev/profitfirst/practice/src/server/services/expense-service.ts (logic to adapt, incl. resolveCategoryName + soft delete)
    - /mnt/c/dev/profitfirst/practice/src/server/routes/expenses.ts (route ordering — /:id/restore before /:id)
    - /mnt/c/dev/profitfirst/practice/src/server/validations/expense.ts
    - apps/api/src/services/auth-service.ts (factory, HTTPException, ownership scoping)
    - apps/api/src/routes/auth.ts (zValidator 422 hook, createDb-in-handler)
    - apps/api/src/routes/expenses.ts (Plan-01 stub — fill, keep export `expensesRouter`)
    - apps/api/tests/helpers/db.ts (createTestDb)
    - .planning/phases/02-income-expenses/02-RESEARCH.md §"Zod Schemas", §"Pattern 7: Drizzle Soft Delete", §Security Domain
    - .planning/phases/02-income-expenses/02-PATTERNS.md §expense-service, §expenses route, §expense.test
    - .planning/phases/02-income-expenses/02-CONTEXT.md D-10 (paymentMethod validated enum, optional)
  </read_first>
  <behavior>
    - EXP-01: create(userId, input) inserts amount in cents, categoryName resolved from owned categoryId, paymentMethod nullable (one of cash/gcash/bank_transfer/maya/check when present), deletedAt null.
    - EXP-02: list(userId, { page, limit, from, to }) returns { content, page, last }; date range filters expenseDate; ordered expenseDate desc. List shows active rows for totals; deleted rows surfaced separately or via a flag (Claude's discretion — must allow the UI to display + restore deleted rows). Totals/sum must use `isNull(expenses.deletedAt)`.
    - EXP-03: update(id, userId, partial) updates owned row only; 404 otherwise; re-resolves categoryName on categoryId change.
    - EXP-04: delete(id, userId) is SOFT — sets `deletedAt = new Date().toISOString()`; restore(id, userId) clears deletedAt (null). Both 404 if not owned. Soft-deleted rows are excluded from totals.
    - IDOR: cross-user access → 404, never mutates.
  </behavior>
  <action>
    Create apps/api/src/schemas/expense.ts: `PAYMENT_METHOD_VALUES = ['cash','gcash','bank_transfer','maya','check'] as const`; `createExpenseSchema` (categoryId positive int, amount positive number, description max 500 optional, expenseDate /^\d{4}-\d{2}-\d{2}$/, paymentMethod z.enum(PAYMENT_METHOD_VALUES).optional().nullable() per D-10), `updateExpenseSchema = createExpenseSchema.partial()`, `expenseQuerySchema` (page/limit + optional from/to). amount is cents from the web layer; keep `.positive()`.

    Create apps/api/src/services/expense-service.ts: `export function createExpenseService(db)` with list/create/getById/update/delete(soft)/restore per <behavior>. resolveCategoryName validates expenseCategories ownership (400 invalid_category if missing). delete uses `db.update(expenses).set({ deletedAt: new Date().toISOString() }).where(and(eq(expenses.id,id), eq(expenses.userId,userId)))`; restore sets deletedAt null. Totals/sum queries add `isNull(expenses.deletedAt)`. All scoped to userId; scoped miss → HTTPException(404, not_found). No `any`.

    Fill apps/api/src/routes/expenses.ts (Plan-01 stub): register on `expensesRouter` with the auth.ts validation pattern. Order: GET '/' (query validated), POST '/' (json, 201), PATCH '/:id/restore' — REGISTER BEFORE '/:id', GET '/:id', PUT '/:id' (json), DELETE '/:id'. Keep `export { expensesRouter }`.

    Create apps/api/tests/expense.test.ts via createTestDb() + createExpenseService(testDb) covering EXP-01..04 + IDOR: assert cents stored correctly; date-range filter; update; soft delete sets deletedAt and the row is excluded from active list/total; restore clears deletedAt and the row returns; cross-user access → not_found; paymentMethod rejects an invalid value at the schema level.

  </action>
  <verify>
    <automated>cd apps/api && npm run test -- expense && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/schemas/expense.ts exports createExpenseSchema, updateExpenseSchema, expenseQuerySchema; paymentMethod uses z.enum of exactly the 5 values
    - apps/api/src/services/expense-service.ts exports createExpenseService; delete() sets deletedAt (soft); restore() sets deletedAt null; totals use `isNull(expenses.deletedAt)`; every query contains `eq(expenses.userId`
    - apps/api/src/routes/expenses.ts registers `PATCH '/:id/restore'` before `'/:id'` handlers and `export { expensesRouter }`
    - apps/api/tests/expense.test.ts asserts soft-delete exclusion + restore + IDOR not_found
    - `cd apps/api && npm run test -- expense` exits 0 and `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Expense API slice implemented with soft delete/restore, userId-scoped, EXP-01..04 + IDOR tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Expense record + browse UI (page, form w/ payment method, list, date filter, load-more, create action)</name>
  <files>apps/web/src/app/expenses/page.tsx, apps/web/src/app/expenses/new/page.tsx, apps/web/src/app/expenses/new/_actions/create-expense.ts, apps/web/src/app/expenses/_components/expenses-overview.tsx, apps/web/src/app/expenses/_components/expense-list.tsx, apps/web/src/app/expenses/_components/expense-form.tsx</files>
  <read_first>
    - /mnt/c/dev/profitfirst/practice/src/app/(dashboard)/expenses/ (page/form/list structure to mirror)
    - apps/web/src/app/(auth)/login/page.tsx (RSC default-export + getSession)
    - apps/web/src/components/auth/LoginForm.tsx ('use client', toast, useTransition)
    - apps/web/src/server/api.ts, apps/web/src/lib/format-currency.ts, apps/web/src/lib/format-date.ts, apps/web/src/lib/constants.ts (Plan 01 — PAYMENT_METHODS)
    - .planning/phases/02-income-expenses/02-RESEARCH.md §"Pattern 5: Load-More", §Pitfall 2 (cents)
    - .planning/phases/02-income-expenses/02-PATTERNS.md §expenses page/overview/form/list
    - .planning/phases/02-income-expenses/02-CONTEXT.md D-07 (expense list = date-range filter only), D-09/D-10 (payment methods), Claude's Discretion (deleted-row surfacing/restore UI)
  </read_first>
  <action>
    apps/web/src/app/expenses/page.tsx (RSC default export): getSession→redirect('/login'); apiFetch GET '/api/expenses?page=0&limit=20' and GET '/api/expense-categories'; render `<ExpensesOverview initialData=... categories=... />`. Read searchParams for SSR date filter.

    apps/web/src/app/expenses/new/page.tsx (RSC): getSession; fetch expense categories; render `<ExpenseForm categories=... action={createExpenseAction} />`.

    apps/web/src/app/expenses/new/_actions/create-expense.ts ('use server') `createExpenseAction(formData)`: `toCents(Number(formData.get('amount')))` (Pitfall 2); body {categoryId, amount, description, expenseDate, paymentMethod: formData.get('paymentMethod') || null}; apiFetch POST '/api/expenses'; catch ApiError→{error}; success → revalidatePath('/expenses') + redirect('/expenses').

    apps/web/src/app/expenses/_components/expense-form.tsx ('use client'): category select, amount (decimal pesos), date input (default today), payment method select populated from `PAYMENT_METHODS` (optional — allow blank), description textarea. Submit via passed action + useTransition + sonner toast. Reusable for /new and edit (accept `action` + optional `initialValues`).

    apps/web/src/app/expenses/_components/expense-list.tsx ('use client'): rows with formatCurrency amount, formatDate expenseDate, payment-method label. Active rows clickable → edit dialog. Soft-deleted rows surfaced (Claude's discretion — e.g. a "show deleted" toggle or greyed rows) with an inline restore (RotateCcw icon) calling restoreExpenseAction. Empty state.

    apps/web/src/app/expenses/_components/expenses-overview.tsx ('use client'): composes a date-range filter (nuqs useQueryState — D-07, no text search for expenses), the list, a "Load more" button (D-06 — append-on-demand, server-action-fetched next page appended; reset accumulator on filter change, NOT numbered pages or infinite scroll), and an optional totals header summing ACTIVE (non-deleted) amounts via formatCurrency.

    shadcn primitives only; no hand edits to components/ui/*; aliases only.

  </action>
  <verify>
    <automated>cd apps/web && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - apps/web/src/app/expenses/page.tsx is a default-export async RSC calling getSession and apiFetch('/api/expenses...')
    - create-expense.ts contains `toCents(` and `revalidatePath('/expenses')`
    - expense-form.tsx populates the payment-method select from `PAYMENT_METHODS` and allows blank (optional)
    - expenses-overview.tsx uses a date-range filter (no free-text search) and a "Load more" affordance
    - expense-list.tsx renders a restore affordance for soft-deleted rows
    - `cd apps/web && npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>A user can open /expenses (date-filtered list + load-more) and /expenses/new to record an expense with a payment method; create persists.</done>
</task>

<task type="auto">
  <name>Task 3: Edit + soft-delete + restore expense (dialog + server actions)</name>
  <files>apps/web/src/app/expenses/_components/edit-expense-dialog.tsx, apps/web/src/app/expenses/_components/expense-actions.ts</files>
  <read_first>
    - apps/web/src/app/expenses/_components/expense-form.tsx (Task 2 — reuse for edit)
    - apps/web/src/app/expenses/_components/expenses-overview.tsx / expense-list.tsx (Task 2 — row→dialog + restore wiring)
    - apps/web/src/app/expenses/new/_actions/create-expense.ts (Task 2 — apiFetch/ApiError/revalidate pattern)
    - /mnt/c/dev/profitfirst/practice/src/app/(dashboard)/expenses/ (edit dialog structure)
    - .planning/phases/02-income-expenses/02-CONTEXT.md D-05 (edit-in-dialog), D-12 awareness (delete here is soft, not category)
    - .planning/phases/02-income-expenses/02-PATTERNS.md §edit-expense-dialog, §expense-actions
  </read_first>
  <action>
    apps/web/src/app/expenses/_components/expense-actions.ts ('use server'): `updateExpenseAction(id, formData)` → PUT '/api/expenses/:id' (toCents on amount, paymentMethod || null), `deleteExpenseAction(id)` → DELETE '/api/expenses/:id' (soft delete server-side), `restoreExpenseAction(id)` → PATCH '/api/expenses/:id/restore'. Each: apiFetch, catch ApiError→{error}, success → revalidatePath('/expenses').

    apps/web/src/app/expenses/_components/edit-expense-dialog.tsx ('use client', D-05): shadcn Dialog from a list row reusing expense-form.tsx with the row's initialValues bound to `updateExpenseAction`; Delete button (soft delete) with a confirm step calling deleteExpenseAction; sonner toast + useTransition; close + revalidate refresh.

    Wire delete/edit into expense-list/expenses-overview; wire restoreExpenseAction into the deleted-row restore affordance from Task 2. Aliases only; no hand edits to components/ui/*.

  </action>
  <verify>
    <automated>cd apps/web && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - expense-actions.ts is 'use server' and exports updateExpenseAction, deleteExpenseAction, restoreExpenseAction; restoreExpenseAction calls PATCH on `/api/expenses/${id}/restore`
    - edit-expense-dialog.tsx reuses expense-form and includes a (soft) delete action with confirm
    - all three actions call `revalidatePath('/expenses')` on success
    - `cd apps/web && npm run typecheck` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>From the expense list a user can edit, soft-delete (with confirm), and restore — EXP-03/04 complete in the UI.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full expense slice: record an expense with a payment method, browse with a date-range filter + load-more, edit in a dialog, soft-delete (excluded from totals), and restore.</what-built>
  <how-to-verify>
    1. Start dev (`npm run dev`), log in, visit http://localhost:3006/expenses.
    2. /expenses/new → record an expense (category, decimal amount e.g. 250.75, date, payment method = GCash, description) — confirm it shows as ₱250.75 and GCash.
    3. Record several expenses, set a date-range filter — confirm filtering + that the loaded list resets; click "Load more" to confirm append.
    4. Confirm a totals header reflects only active expenses.
    5. Edit a row (change amount) → save → confirm update.
    6. Delete a row → confirm → it disappears from active list and the total drops; reveal deleted rows (toggle) → restore it → confirm it returns and the total goes back up.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                | Description                                          |
| ----------------------- | ---------------------------------------------------- |
| browser → server action | expense form data crosses to server                  |
| Next.js → Workers API   | access token forwarded; API authorizes               |
| expense service → D1    | userId-scoped reads/writes incl. soft delete/restore |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                | Disposition | Mitigation Plan                                                                                                                  |
| --------- | ---------------------- | -------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------- |
| T-02-10   | Elevation of Privilege | IDOR — reading/mutating/restoring another user's expense | mitigate    | every expense-service query (incl. restore) includes `eq(expenses.userId, userId)`; scoped miss → 404 (Task 1, asserted by test) |
| T-02-11   | Tampering              | negative/fractional amount                               | mitigate    | Zod `amount.positive()`; integer cents via toCents (Task 1/2)                                                                    |
| T-02-12   | Tampering              | arbitrary paymentMethod string                           | mitigate    | D-10: Zod z.enum restricts to the 5 known values (Task 1)                                                                        |
| T-02-13   | Tampering              | categoryId from another user                             | mitigate    | resolveCategoryName validates expenseCategories ownership → 400 invalid_category (Task 1)                                        |
| T-02-14   | Information Disclosure | soft-deleted expense counted in totals                   | mitigate    | totals/sum filtered with `isNull(expenses.deletedAt)` (Task 1)                                                                   |
| T-02-15   | Spoofing               | unauthenticated expense access                           | mitigate    | /api/expenses/\* behind requireAuth (Plan 01); /expenses getSession→redirect (Task 2)                                            |
| T-02-SC   | Tampering              | npm installs                                             | accept      | No new packages this phase                                                                                                       |

</threat_model>

<verification>
- `cd apps/api && npm run test -- expense` green (EXP-01..04 + IDOR + soft delete/restore)
- `cd apps/api && npm run typecheck` green
- `cd apps/web && npm run typecheck && npm run lint` green
- Human checkpoint passes the expense flow end to end
</verification>

<success_criteria>

- EXP-01..04 implemented DB→API→UI; soft delete excludes from totals + restorable; userId-scoped; tests pass; human verifies the live flow
  </success_criteria>

<output>
Create `.planning/phases/02-income-expenses/02-03-SUMMARY.md` when done.
</output>
