---
phase: 05-dashboard
plan: 05
type: execute
wave: 1
depends_on: ['05-04']
files_modified:
  - apps/web/src/app/(dashboard)/overview/_components/overview-filters.tsx
  - apps/web/src/app/(dashboard)/overview/_components/overview-content.tsx
  - apps/web/src/app/(dashboard)/overview/page.tsx
autonomous: true
gap_closure: true
requirements: [DASH-01]

must_haves:
  truths:
    - 'The /overview page surfaces income totals (received + pending), expense total, net income, total wallet balance, and per-account Profit First computedBalance — all governed by one date filter'
    - 'The recent feed is unified: income + expense + wallet deposit/withdrawal rows, each row a Link navigating to /income, /expenses, or /wallets/{walletId}'
    - 'A date filter (This Month default, Manila tz, nuqs URL params) governs every figure and the feed; the wallet balance is period-scoped with an explicit period sub-label'
    - 'The feed paginates via a client-side Load more button through the /api/dashboard BFF proxy; the button hides when there are no more rows'
    - 'A brand-new user still sees the adopted welcome/first-run state; otherwise zeroed figures render as ₱0.00 when a fetch fails'
  artifacts:
    - path: 'apps/web/src/app/(dashboard)/overview/_components/overview-filters.tsx'
      provides: 'date-preset filter (nuqs) + getDefaultOverviewRange export'
      contains: 'DATE_PRESETS'
    - path: 'apps/web/src/app/(dashboard)/overview/_components/overview-content.tsx'
      provides: 'adopted overview layout extended with stat figures, PF balances, unified navigable feed, Load more'
      contains: 'useAmountVisibility'
    - path: 'apps/web/src/app/(dashboard)/overview/page.tsx'
      provides: 'RSC fetching GET /api/dashboard/summary with date range, replacing the four-endpoint fan-out'
      contains: 'dashboard/summary'
  key_links:
    - from: 'apps/web/src/app/(dashboard)/overview/page.tsx'
      to: '/api/dashboard/summary'
      via: 'apiFetch with from/to query, cache no-store'
      pattern: 'dashboard/summary'
    - from: 'apps/web/src/app/(dashboard)/overview/_components/overview-content.tsx'
      to: '/api/dashboard/summary?feedPage='
      via: 'client-side Load more fetch via BFF'
      pattern: 'feedPage='
    - from: 'apps/web/src/app/(dashboard)/overview/_components/overview-content.tsx'
      to: 'tx.href'
      via: 'feed row Link navigation'
      pattern: '<Link href=\\{'
---

<objective>
Close the seven UI gaps on the ADOPTED `/overview` page by rewiring it to the new `GET /api/dashboard/summary` endpoint (Plan 05-04) and extending the adopted layout — NOT replacing it. The adopted design language (hero balance, monochrome ink ramp, minimal whitespace-and-type aesthetic, `PfAllocationBar`, the first-run welcome) is the base; this plan adds the missing financial substance into that idiom.

Gaps closed (from 05-VERIFICATION.md):

1. Income totals (received + pending), expense total, net income surfaced as figures.
2. Per-account Profit First computedBalance displayed via MaskedAmount.
3. Wallet transactions (DEPOSIT/WITHDRAWAL) included in the recent feed.
4. Date filter (nuqs, This Month default, Manila tz) governing all sections.
5. Period-scoped wallet balance with an explicit period sub-label.
6. Load-more feed pagination (client-side state via the BFF proxy).
7. Feed rows are navigable Links.

ACCEPTED DEVIATIONS (do not "fix"): the route is `/overview` (not `/dashboard`); the nav label is "Overview". The summary endpoint returns feed `href` values of `/income`, `/expenses`, `/wallets/{id}` (D-05) — use them as-is.

Purpose: DASH-01 user-facing surface (D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-09, D-12). No new dependencies.
Output: The adopted `/overview` page now renders real period-scoped financial figures, PF balances, a unified navigable feed with Load more, all driven by one date filter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-PATTERNS.md
@.planning/phases/05-dashboard/05-UI-SPEC.md

<interfaces>
<!-- Contracts the executor must build against — produced by Plan 05-04 + the adopted codebase. Do not re-explore. -->

From apps/web/src/types/dashboard.ts (Plan 05-04 output):

- `type RecentTransactionKind = 'income' | 'expense' | 'wallet_deposit' | 'wallet_withdrawal'`
- `interface RecentTransaction { id: number; kind: RecentTransactionKind; date: string; amountCents: number; description: string | null; label: string; href: string; }`
- `interface FeedPagination { page: number; size: number; hasMore: boolean; }`
- `interface DashboardSummary { totalIncomeReceivedCents; totalIncomePendingCents; totalExpensesCents; netIncomeCents; totalWalletBalanceCents; profitFirstAccounts: PfAccount[]; totalIncome: number; recentTransactions: RecentTransaction[]; feedPagination: FeedPagination; }`

ADOPTED base files (extend, do not rewrite from scratch):

- apps/web/src/app/(dashboard)/overview/page.tsx — currently: getSession() guard → redirect('/login') if no session; Promise.all over /api/wallets, /api/profit-first/summary, /api/incomes, /api/expenses; builds a `recent: RecentEntry[]`; computes all-time totalBalanceCents; renders `<OverviewContent greeting totalBalanceCents hasWallets accounts totalIncomeCents recent />`. REPLACE the four-endpoint fan-out with a single apiFetch to `/api/dashboard/summary?from=&to=`. KEEP the getSession()/redirect('/login') guard and greetingForNow() helper.
- apps/web/src/app/(dashboard)/overview/\_components/overview-content.tsx — `'use client'`; owns `useAmountVisibility()` and `useRecordSheet()`; has a `RecentEntry` type (kind 'income'|'expense' only); `isFirstRun` welcome state; hero MaskedAmount; `PfAllocationBar` "Your split" section with a per-account legend (color dot + name + targetPercentage%); a "Recent" section rendering `<li>` rows (NOT Links). EXTEND this component.

Reusable primitives (do NOT rebuild):

- `@/components/amount-visibility`: `useAmountVisibility()` → { visible, toggle, mounted }; `<AmountToggle visible toggle />`; `<MaskedAmount cents visible mounted className />`. Always pass BOTH visible and mounted (Pitfall 3).
- `@/lib/format-currency`: `formatCurrency(cents)`. `@/lib/format-date`: `formatDate(dateStr)`. `@/lib/utils`: `cn(...)`.
- `@/server/api`: `apiFetch<T>(path)` — server-side fetch attaching the Bearer token; throws ApiError on non-ok. `@/server/auth`: `getSession()`.
- `@/app/(dashboard)/profit-first/_components/pf-allocation-bar`: `<PfAllocationBar accounts />` (already used by the adopted page — keep it).
- `@/app/(dashboard)/profit-first/_components/pf-overview`: `interface PfAccount { id; name; targetPercentage; color; accountType; computedBalance; ... }`.
- `@/components/RecordSheetProvider`: `useRecordSheet()` → { openRecordSheet } (adopted first-run/empty CTA — keep).

Analogs for the date filter (read for exact pattern):

- apps/web/src/app/(dashboard)/profit-first/\_components/pf-filters.tsx — DATE_PRESETS array (This Month / Last Month / Last 3 Months / This Year / All Time), nowManila()/fmt() helpers (APP_TIMEZONE='Asia/Manila', date-fns + @date-fns/tz/TZDate), useQueryState('from'|'to', parseAsString), isPresetActive, selectPreset + router.refresh().
- .planning/phases/05-dashboard/05-PATTERNS.md §DashboardFilters.tsx — full DATE_PRESETS block + getDefault range + render; §DashboardContent.tsx — Load more handler, feed row Link, color conventions.
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: overview-filters.tsx — date presets (nuqs) + getDefaultOverviewRange</name>
  <files>apps/web/src/app/(dashboard)/overview/_components/overview-filters.tsx</files>
  <read_first>
    - apps/web/src/app/(dashboard)/profit-first/_components/pf-filters.tsx (copy the DATE_PRESETS block, nowManila/fmt helpers, useQueryState, isPresetActive, selectPreset + router.refresh — OMIT the category multi-select Sheet)
    - apps/web/src/app/(dashboard)/overview/_components/overview-content.tsx (the adopted visual idiom — match its quiet aesthetic: text links, ink tones, not heavy filled buttons unless that reads consistently)
    - .planning/phases/05-dashboard/05-PATTERNS.md (§DashboardFilters.tsx — full DATE_PRESETS block + getDefault range pattern)
    - .planning/phases/05-dashboard/05-UI-SPEC.md (§Date Filter copy, §Interaction States active preset)
  </read_first>
  <action>
    Create a `'use client'` `OverviewFilters` component. Copy the DATE_PRESETS const (This Month, Last Month, Last 3 Months, This Year, All Time) and the nowManila()/fmt() helpers verbatim from pf-filters.tsx (APP_TIMEZONE = 'Asia/Manila', date-fns + @date-fns/tz/TZDate). Export `getDefaultOverviewRange(): { from: string; to: string }` returning `DATE_PRESETS[0].getRange()` (This Month) — page.tsx imports this. Use nuqs `useQueryState('from'|'to', parseAsString)`, `isPresetActive(label)` (All Time when !from && !to; otherwise from===range.from && to===range.to), and `selectPreset(label)` that sets from/to to the range value or null then calls router.refresh(). Render the preset buttons in a `flex items-center gap-2 flex-wrap` row; active preset visually distinct from inactive (per UI-SPEC: active=`variant="default"`, inactive=`variant="outline"`, `size="sm"` — OR, to match the adopted quiet aesthetic, render active as ink-strong text and inactive as ink-faint text; choose one and apply it consistently). Do NOT include the category picker or the Sheet. Do NOT modify pf-filters.tsx. nuqs hooks live only here (client) — never in page.tsx (Pitfall 2).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "overview-filters" | head -5; test -f "apps/web/src/app/(dashboard)/overview/_components/overview-filters.tsx" && echo EXISTS</automated>
  </verify>
  <acceptance_criteria>
    - File exists and starts with `'use client';`.
    - Exports `getDefaultOverviewRange` and an `OverviewFilters` component (`grep -c "export" overview-filters.tsx` >= 2).
    - Contains all five preset labels: "This Month", "Last Month", "Last 3 Months", "This Year", "All Time".
    - `grep -cE "useQueryState|TZDate|Asia/Manila" overview-filters.tsx` confirms nuqs + Manila tz present; uses `router.refresh()`.
    - pf-filters.tsx is unchanged (git diff shows no edits to that file).
  </acceptance_criteria>
  <done>Date-only filter renders five Manila-tz presets, syncs from/to to the URL, and exports getDefaultOverviewRange for the page.</done>
</task>

<task type="auto">
  <name>Task 2: overview-content.tsx — stat figures, PF balances, unified navigable feed, Load more</name>
  <files>apps/web/src/app/(dashboard)/overview/_components/overview-content.tsx</files>
  <read_first>
    - apps/web/src/app/(dashboard)/overview/_components/overview-content.tsx (the file being modified — preserve isFirstRun welcome, hero MaskedAmount, PfAllocationBar "Your split" section, ink tone classes text-ink/text-ink-soft/text-ink-faint/text-income/text-expense)
    - apps/web/src/app/(dashboard)/overview/_components/overview-filters.tsx (OverviewFilters + getDefaultOverviewRange — Task 1 output)
    - apps/web/src/types/dashboard.ts (DashboardSummary / RecentTransaction — Plan 05-04 output)
    - apps/web/src/components/amount-visibility.tsx (useAmountVisibility/MaskedAmount/AmountToggle exact API)
    - apps/web/src/app/(dashboard)/profit-first/_components/pf-overview.tsx (PfAccount fields, especially computedBalance, color, name, targetPercentage)
    - apps/web/src/lib/format-currency.ts and apps/web/src/lib/format-date.ts
    - .planning/phases/05-dashboard/05-PATTERNS.md (§DashboardContent.tsx — Load more handler, feed row Link, emerald/rose amount color convention)
    - .planning/phases/05-dashboard/05-UI-SPEC.md (§Stat Card sub-labels/copy, §Feed Row, §Load More, §Empty Feed State, §Copywriting Contract)
  </read_first>
  <action>
    Rework the props to consume the summary. Change `OverviewContentProps` to `{ greeting: string; summary: DashboardSummary | null; from: string; to: string }`. Remove the old `RecentEntry` type and the `totalBalanceCents`/`hasWallets`/`accounts`/`totalIncomeCents`/`recent` props (they are now fields of `summary`). Keep `useAmountVisibility()` and `useRecordSheet()`.

    Derive: `accounts = summary?.profitFirstAccounts ?? []`, `walletBalanceCents = summary?.totalWalletBalanceCents ?? 0`, and feed state in useState (NOT URL — Pitfall 5): `feedItems` (init summary?.recentTransactions ?? []), `hasMore` (init summary?.feedPagination.hasMore ?? false), `feedPage` (init 0), `loadingMore`. Compute `isFirstRun` from the summary being effectively empty: all five figures 0 AND accounts.length === 0 AND feedItems.length === 0 — keep the adopted welcome block unchanged for that case.

    Header: keep the greeting + hero. The hero MaskedAmount now shows `walletBalanceCents` (period-scoped). Render `<OverviewFilters />` and the existing `<AmountToggle visible toggle />` near the top so the user can change the period and toggle masking. Add a period sub-label near the hero that interpolates the range to make period-scope explicit (D-02), e.g. text reading "Period total · {from} – {to}" in the adopted ink-faint style (replace or augment the existing "across all wallets" sub-label). Keep the empty-wallet "set them up" link behavior when there are no wallets.

    Stat figures (D-01) — integrate into the adopted idiom, NOT a heavy 5-card grid that clashes. Render a compact figures row/section below the hero showing: Total Income (received) = totalIncomeReceivedCents, Pending Income = totalIncomePendingCents, Total Expenses = totalExpensesCents, Net Income = netIncomeCents. Each figure: a small ink-faint label (use the UI-SPEC copy — "Total Income"/"Received this period", "Pending Income"/"Awaiting receipt", "Total Expenses"/"Recorded this period", "Net Income"/"Income minus expenses") and a `<MaskedAmount cents visible mounted className>` value in tabular-nums. Net Income value adds `text-expense` (the adopted negative tone) when `netIncomeCents < 0`. Use a layout consistent with the adopted minimal aesthetic (e.g. a `grid grid-cols-2 sm:grid-cols-4 gap-x-6 gap-y-4` of label+amount stacks, ink tones, no colored icon chips) — declare and apply one concrete layout; do not improvise card styling that conflicts with the monochrome design. Use `?? 0` for every cents value so summary=null renders ₱0.00 (D-12 zeroed).

    Profit First section: keep the adopted `PfAllocationBar` "Your split" section (percentages) AND extend the per-account legend list so each account row also shows its `computedBalance` via `<MaskedAmount cents={account.computedBalance} visible mounted className="text-ink-soft tabular-nums">` alongside the existing color dot + name + targetPercentage% (gap 2). This surfaces the per-account derived balance the verification requires without discarding the adopted split bar.

    Recent feed (gaps 3, 5-skip, 6, 7): rename the section to render `feedItems` (RecentTransaction[]) from the summary. Each row is now a `<Link href={tx.href}>` (D-05 navigation) styled to match the adopted ledger row (`flex items-center gap-4 py-3.5`, label `truncate text-sm font-medium`, date via formatDate in ink-faint), adding a hover affordance (e.g. `-mx-2 rounded-md px-2 hover:bg-paper-deep/40 transition-colors`). The amount uses `<MaskedAmount cents={tx.amountCents} visible mounted className>` with a leading sign and color by kind: income/wallet_deposit → `+` and the adopted `text-income`; expense/wallet_withdrawal → `−` and `text-expense`. Add a small type label per row (Income / Expense / Deposit / Withdrawal) derived from `tx.kind`, in the adopted quiet style. Keep the "Income"/"Expenses" section header links. Wallet transactions now appear because the endpoint includes them — no extra fetch needed.

    Load more (D-04): when `hasMore`, render a centered Load more control below the feed (button or text link consistent with the adopted aesthetic) labeled "Load more" (disabled + "Loading…" while loadingMore). `handleLoadMore`: fetch `/api/dashboard/summary?from=${from}&to=${to}&feedPage=${feedPage+1}&feedSize=20` via the same-origin BFF proxy (Plan 05-04 created it); on !res.ok `toast.error('Could not load more transactions. Please try again.')`; on success append `json.data.recentTransactions` to feedItems, set hasMore from `json.data.feedPagination.hasMore`, increment feedPage. Keep pagination in useState only — never a nuqs URL param (Pitfall 5). Hide the control when hasMore is false (remove from DOM, do not just disable). Import `toast` from 'sonner'.

    Empty feed (non-first-run but feedItems empty): keep the adopted "Nothing recorded yet" empty state OR show getting-started CTAs to /income/new, /expenses/new, /wallets/new per D-12 — match the adopted button style; at minimum keep a usable empty state, do not leave it blank.

  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "overview-content" | head -10; echo "---"; cd apps/web && npx eslint "src/app/(dashboard)/overview/_components/overview-content.tsx" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - File still begins with `'use client';` and imports `useAmountVisibility`, `MaskedAmount`, `AmountToggle` from `@/components/amount-visibility` and `DashboardSummary`/`RecentTransaction` from `@/types/dashboard`.
    - Props include `summary` (DashboardSummary | null), `from`, `to`; the old `recent: RecentEntry[]` prop is gone (`grep -c "RecentEntry" overview-content.tsx` returns 0).
    - All four income/expense figures are present by label: `grep -cE "Total Income|Pending Income|Total Expenses|Net Income" overview-content.tsx` >= 4.
    - Net Income amount applies the negative tone conditionally on `netIncomeCents < 0`.
    - Per-account computedBalance is rendered (`grep -c "computedBalance" overview-content.tsx` >= 1).
    - Feed rows are Links: `grep -c "<Link href={tx.href}" overview-content.tsx` >= 1 (rows navigate per D-05).
    - Load more fetch URL contains `feedPage=`; feed pagination held in `useState` (`grep -c "useQueryState" overview-content.tsx` returns 0).
    - Period sub-label interpolates `from` and `to` (D-02 period-scope visible).
    - tsc and eslint report no errors for this file.
  </acceptance_criteria>
  <done>The adopted /overview layout now shows period-scoped income/expense/net figures, per-account PF computedBalance, a unified navigable feed (income + expense + wallet tx) with client-side Load more, all masked via the shared hook.</done>
</task>

<task type="auto">
  <name>Task 3: overview/page.tsx — fetch /api/dashboard/summary, drop the four-endpoint fan-out</name>
  <files>apps/web/src/app/(dashboard)/overview/page.tsx</files>
  <read_first>
    - apps/web/src/app/(dashboard)/overview/page.tsx (the file being modified — keep getSession()/redirect('/login') guard and greetingForNow(); replace the Promise.all fan-out)
    - apps/web/src/app/(dashboard)/overview/_components/overview-filters.tsx (import getDefaultOverviewRange — Task 1 output)
    - apps/web/src/app/(dashboard)/overview/_components/overview-content.tsx (new props { greeting, summary, from, to } — Task 2 output)
    - apps/web/src/types/dashboard.ts (DashboardSummary — Plan 05-04 output)
    - apps/web/src/server/api.ts (apiFetch signature; it attaches the Bearer token and throws ApiError on non-ok)
    - apps/web/src/app/(dashboard)/profit-first/page.tsx (RSC searchParams await + no-store reference if apiFetch caching needs confirming)
    - .planning/phases/05-dashboard/05-PATTERNS.md (§page.tsx — searchParams + default range + server fetch)
  </read_first>
  <action>
    Change the page signature to accept searchParams: `export default async function OverviewPage({ searchParams }: { searchParams: Promise<{ from?: string; to?: string }> })`. Keep the existing `getSession()` guard and `redirect('/login')` when no session. Keep `greetingForNow()`.

    `const params = await searchParams;` Compute the period: `const defaults = getDefaultOverviewRange(); const from = params.from ?? defaults.from; const to = params.to ?? defaults.to;` (This Month, Manila — D-08).

    Replace the four-endpoint Promise.all (wallets / profit-first/summary / incomes / expenses) and all the `recent`/`totalBalanceCents` assembly with a SINGLE call: `const summary = await apiFetch<{ data: DashboardSummary }>(\`/api/dashboard/summary?from=${from}&to=${to}\`).then((r) => r.data).catch(() => null);`. apiFetch goes server-to-server with the Bearer token. If apiFetch does not already set no-store, pass `{ cache: 'no-store' }` so data is fresh per visit (D-09) — match the profit-first page's freshness guarantee. On any error, summary is null and the content renders zeroed (D-12).

    Render `<OverviewContent greeting={greetingForNow()} summary={summary} from={from} to={to} />`. Remove now-unused imports (WalletListItem, Income/IncomeListResponse, the SummaryResponse/ExpenseItem/PaginatedExpenses local interfaces, the RecentEntry import) — keep only what the new flow uses. Do NOT use nuqs here (RSC — Pitfall 2).

  </action>
  <verify>
    <automated>cd apps/api && npx vitest run && cd ../web && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -15 && cd ../web && npm run lint 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx default-exports an async OverviewPage that accepts `searchParams` and awaits it.
    - The fetch targets `/api/dashboard/summary?from=` (`grep -c "dashboard/summary" page.tsx` >= 1) and the old `/api/incomes`, `/api/expenses`, `/api/wallets`, `/api/profit-first/summary` fan-out is removed (`grep -cE "/api/incomes|/api/expenses|/api/wallets" page.tsx` returns 0).
    - Imports `getDefaultOverviewRange` from `./_components/overview-filters` and the `getSession()` guard with `redirect('/login')` is retained.
    - `grep -cE "useQueryState|'use client'" page.tsx` returns 0 (RSC, no nuqs).
    - `cd apps/web && npx tsc --noEmit` and `npm run lint` pass; `cd apps/api && npx vitest run` still green.
  </acceptance_criteria>
  <done>/overview server-renders fresh summary data (one endpoint) into the adopted layout, applying the This Month default range and the URL date filter.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                   |
| ------------------ | --------------------------------------------------------------------------------------------- |
| browser → Next RSC | searchParams (from/to) are untrusted; passed only to the API which Zod-validates them         |
| client → BFF proxy | Load more fetch crosses /api/dashboard/\* same-origin; BFF forwards the httpOnly access_token |

## STRIDE Threat Register

| Threat ID | Category               | Component              | Disposition | Mitigation Plan                                                                                                                                      |
| --------- | ---------------------- | ---------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-06   | Spoofing               | /overview route        | mitigate    | page.tsx calls getSession() and redirect('/login') when absent; middleware also guards /overview (not a PUBLIC_ROUTE); identity from httpOnly cookie |
| T-05-07   | Tampering              | from/to in fetch URL   | mitigate    | Forwarded to the API which validates them with the Zod date regex (T-05-02); UI never builds SQL                                                     |
| T-05-08   | Information Disclosure | Load more client fetch | mitigate    | Goes through the BFF proxy which attaches the user's own Bearer token; API scopes results to that userId (T-05-03)                                   |
| T-05-09   | Information Disclosure | Amount masking         | accept      | MaskedAmount renders masked on SSR (mounted guard) — values never leak before the user toggles visibility; cosmetic, not a security control          |

</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` and `npm run lint` — clean.
- `cd apps/api && npx vitest run` — backend suite still green.
- Manual (human-verify): log in, land on /overview, confirm income/pending/expense/net figures + per-account PF balances + a unified feed (including a wallet deposit/withdrawal) match seeded data; change the date preset and confirm all figures + feed + wallet balance update and the wallet balance period sub-label changes; click Load more; click a feed row and confirm navigation to /income, /expenses, or the wallet detail page.
</verification>

<success_criteria>

- /overview renders period-scoped income/expense/net figures, per-account PF computedBalance, and a unified recent-transactions feed including wallet deposits/withdrawals.
- One URL date filter (This Month default, Manila tz) governs the whole page; the wallet balance is period-scoped with an explicit period label.
- Feed rows are Links navigating to /income, /expenses, /wallets/{id}; Load more is client-side via the BFF proxy.
- The adopted visual design is preserved (hero, split bar, ink aesthetic); no new dependencies; tsc + lint clean.
  </success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-05-SUMMARY.md` when done.
</output>
</content>
