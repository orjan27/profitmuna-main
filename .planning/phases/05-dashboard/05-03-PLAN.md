---
phase: 05-dashboard
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/src/components/DashboardNav.tsx
  - apps/web/src/app/page.tsx
autonomous: true
requirements: [DASH-01]

must_haves:
  truths:
    - 'The Dashboard nav entry links to /dashboard (not /), and its active state highlights when on /dashboard'
    - 'All five section links (Dashboard, Income, Expenses, Profit First, Wallets) are present in the nav shell; no Settings entry yet'
    - 'An authenticated user visiting / is redirected to /dashboard; unauthenticated visitors still see the marketing page'
  artifacts:
    - path: 'apps/web/src/components/DashboardNav.tsx'
      provides: 'nav shell with Dashboard → /dashboard'
      contains: '/dashboard'
    - path: 'apps/web/src/app/page.tsx'
      provides: 'root page with authenticated redirect to /dashboard'
      contains: '/dashboard'
  key_links:
    - from: 'apps/web/src/app/page.tsx'
      to: '/dashboard'
      via: 'redirect when access_token cookie present'
      pattern: "redirect\\('/dashboard'\\)"
    - from: 'apps/web/src/components/DashboardNav.tsx'
      to: '/dashboard'
      via: 'Dashboard nav href'
      pattern: "href: '/dashboard'"
---

<objective>
Finalize the navigation shell and landing route for the dashboard (D-10, D-11): point the Dashboard nav entry at `/dashboard`, confirm all section links are wired, and redirect authenticated users from `/` to `/dashboard` while keeping the marketing page for logged-out visitors.

Purpose: D-10 (dashboard at /dashboard; authenticated / redirects there) and D-11 (Phase 5 owns finalizing the nav shell; Settings waits for Phase 6).
Output: Correct nav active-state for /dashboard and a session-aware root redirect. No new dependencies.

This plan is independent of the dashboard API (Plan 01) and the dashboard page UI (Plan 02) — it modifies only DashboardNav.tsx and the root page.tsx, which neither other plan touches. It can run in parallel with Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md
@.planning/phases/05-dashboard/05-PATTERNS.md

<interfaces>
<!-- Current state of the files this plan edits. Extracted from codebase. -->

apps/web/src/components/DashboardNav.tsx (existing, 'use client'):

- NAV_ITEMS array currently: { label: 'Dashboard', href: '/', icon: LayoutDashboard } then Income(/income), Expenses(/expenses), Profit First(/profit-first), Wallets(/wallets).
- isActive logic: `href === '/' ? pathname === '/' : pathname === href || pathname.startsWith(href + '/')`.
- ONLY change needed: Dashboard href '/' → '/dashboard'. Once non-root, the existing startsWith branch handles active detection automatically (no isActive special-case needed for it, though the `href === '/'` branch may be removed since no item uses '/' anymore — keep it harmless or simplify).

apps/web/src/app/page.tsx (existing marketing landing, ~214 lines):

- Currently a server component rendering marketing content.
- Add a session check at the top before rendering: read access_token cookie, redirect('/dashboard') if present; otherwise render the existing marketing content unchanged.

Auth/session helpers available:

- `apps/web/src/server/auth.ts` exports `getSession()` for server components.
- Alternatively read the cookie directly: `import { cookies } from 'next/headers'; const accessToken = (await cookies()).get('access_token')?.value;`.
- `import { redirect } from 'next/navigation';`.

Do NOT modify apps/web/src/middleware.ts — `/` is correctly a PUBLIC_ROUTE; the redirect is page-level only (RESEARCH Pattern 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point Dashboard nav entry to /dashboard</name>
  <files>apps/web/src/components/DashboardNav.tsx</files>
  <read_first>
    - apps/web/src/components/DashboardNav.tsx (current NAV_ITEMS + isActive logic)
    - .planning/phases/05-dashboard/05-PATTERNS.md (§DashboardNav.tsx — the one-line change)
    - .planning/phases/05-dashboard/05-UI-SPEC.md (§Nav Shell copy — five entries, no Settings)
  </read_first>
  <action>
    In NAV_ITEMS change the Dashboard entry from `href: '/'` to `href: '/dashboard'`. Keep the other four entries (Income, Expenses, Profit First, Wallets) unchanged and the order as-is. Do NOT add a Settings entry (Phase 6, D-11). The existing isActive logic already handles non-root hrefs via `pathname === href || pathname.startsWith(href + '/')`; the `href === '/'` special-case branch is now dead for nav items — leaving it is harmless, or simplify isActive to just the non-root form. Confirm exactly five nav entries remain.
  </action>
  <verify>
    <automated>cd apps/web && grep -c "href: '/dashboard'" "src/components/DashboardNav.tsx" && grep -c "href: '/'," "src/components/DashboardNav.tsx"; npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "DashboardNav" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "href: '/dashboard'" apps/web/src/components/DashboardNav.tsx` returns 1.
    - `grep -c "href: '/'," apps/web/src/components/DashboardNav.tsx` returns 0 (no nav item points to root anymore).
    - NAV_ITEMS still contains exactly five entries with labels Dashboard, Income, Expenses, Profit First, Wallets; no "Settings".
    - `cd apps/web && npx tsc --noEmit` reports no errors for DashboardNav.tsx.
  </acceptance_criteria>
  <done>The Dashboard nav link targets /dashboard and highlights as active on that route; the shell has all five section links and no Settings entry.</done>
</task>

<task type="auto">
  <name>Task 2: Root page redirect to /dashboard for authenticated users</name>
  <files>apps/web/src/app/page.tsx</files>
  <read_first>
    - apps/web/src/app/page.tsx (current marketing content — preserve it for logged-out visitors)
    - apps/web/src/server/auth.ts (getSession signature, if used)
    - .planning/phases/05-dashboard/05-PATTERNS.md (§apps/web/src/app/page.tsx — session check + redirect pattern; do not modify middleware.ts)
    - .planning/phases/05-dashboard/05-RESEARCH.md (§Pattern 5 — page-level redirect, middleware unchanged)
  </read_first>
  <action>
    Make the root page an async server component (if not already). At the very top of the component body, read the access_token cookie via `const accessToken = (await cookies()).get('access_token')?.value;` (import cookies from 'next/headers') OR call `await getSession()`. If authenticated, `redirect('/dashboard')` (import redirect from 'next/navigation'). Otherwise render the existing marketing content unchanged. Do NOT modify middleware.ts — `/` stays a PUBLIC_ROUTE; the redirect is page-level only (RESEARCH Pattern 5). Keep all existing marketing markup intact below the guard.
  </action>
  <verify>
    <automated>cd apps/web && grep -c "redirect('/dashboard')" "src/app/page.tsx" && git diff --quiet src/middleware.ts && echo "MIDDLEWARE-UNCHANGED"; npx tsc --noEmit -p tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "redirect('/dashboard')" apps/web/src/app/page.tsx` returns 1.
    - page.tsx imports `redirect` from 'next/navigation' and reads the access_token cookie (or calls getSession).
    - apps/web/src/middleware.ts is unchanged (git diff is empty for that file).
    - Existing marketing content is still present in page.tsx (the redirect is added, not a full replacement).
    - `cd apps/web && npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
  <done>Authenticated users hitting / are redirected to /dashboard; unauthenticated visitors still see the marketing page; middleware untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                      |
| --------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| browser → Next RSC (root /) | Presence of the httpOnly access_token cookie determines redirect; never trusts client-controlled identity claims |

## STRIDE Threat Register

| Threat ID | Category               | Component          | Disposition | Mitigation Plan                                                                                                                                                           |
| --------- | ---------------------- | ------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-10   | Spoofing               | Root redirect gate | mitigate    | Redirect keys off the httpOnly access_token cookie only; the JWT is still validated by the API/middleware on the destination route — the redirect itself grants no access |
| T-05-11   | Elevation of Privilege | Nav links          | accept      | Nav links are plain anchors to routes already guarded by middleware.ts; rendering a link does not bypass auth on the target route                                         |

</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` and `npm run lint` — clean.
- Manual: logged in, visit / → lands on /dashboard; logged out, visit / → marketing page; nav Dashboard link highlights on /dashboard and navigates correctly.
</verification>

<success_criteria>

- Dashboard nav entry points to /dashboard with correct active-state.
- Authenticated / redirects to /dashboard; marketing page preserved for logged-out users.
- Five nav entries, no Settings; middleware.ts unchanged.
  </success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-03-SUMMARY.md` when done.
</output>
