---
phase: 05-dashboard
plan: 06
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/src/app/page.tsx
autonomous: true
gap_closure: true
requirements: [DASH-01]

must_haves:
  truths:
    - 'An authenticated user visiting / is redirected to /overview; unauthenticated visitors still see the marketing page unchanged'
  artifacts:
    - path: 'apps/web/src/app/page.tsx'
      provides: 'root marketing page with an authenticated redirect to /overview'
      contains: '/overview'
  key_links:
    - from: 'apps/web/src/app/page.tsx'
      to: '/overview'
      via: 'redirect when an authenticated session is present'
      pattern: "redirect\\('/overview'\\)"
---

<objective>
Close the last verification gap: authenticated users visiting `/` must be redirected to the dashboard, while unauthenticated visitors keep seeing the marketing page. Per the ACCEPTED DEVIATION for Phase 5, the redirect target is `/overview` (the adopted route), NOT `/dashboard` as D-10 originally said.

Currently `apps/web/src/app/page.tsx` is a pure synchronous marketing server component (`export default function Home()`) with no session check and no redirect — a logged-in user always lands on the marketing page.

This plan touches only `page.tsx`, so it has zero file overlap with Plans 05-04 and 05-05 and runs in parallel with them.

Purpose: D-10 (authenticated `/` redirects to the dashboard). The nav shell already points at `/overview` (verified), so no nav change is needed.
Output: Session-aware root page; marketing preserved for logged-out visitors. No new dependencies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dashboard/05-CONTEXT.md
@.planning/phases/05-dashboard/05-RESEARCH.md

<interfaces>
<!-- Current state of the file this plan edits + the session helper. Extracted from the live codebase. -->

apps/web/src/app/page.tsx (existing marketing landing):

- `export default function Home()` — a SYNCHRONOUS server component rendering marketing markup (hero split demo, steps). ~214 lines. It currently imports only `Link` from 'next/link' and `Button` from '@/components/ui/button'.
- To add the guard: make it `async`, read the session at the very top, redirect when authenticated, otherwise render the existing marketing markup unchanged.

Auth/session helpers available:

- `@/server/auth` exports `async function getSession(): Promise<{ userId: number } | null>` — reads the access_token cookie and decodes it for server-component UI gating (verification is the API's job). Returns null when no/invalid token.
- `import { redirect } from 'next/navigation';`.

Reference: apps/web/src/app/(dashboard)/overview/page.tsx already uses `const session = await getSession(); if (!session) redirect('/login');` — mirror that getSession usage here (inverted: redirect when a session IS present).

Do NOT modify apps/web/src/middleware.ts — `/` is correctly a PUBLIC_ROUTE; the redirect is page-level only (RESEARCH Pattern 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root page redirects authenticated users to /overview</name>
  <files>apps/web/src/app/page.tsx</files>
  <read_first>
    - apps/web/src/app/page.tsx (current marketing content — preserve all of it for logged-out visitors)
    - apps/web/src/server/auth.ts (getSession signature + behavior)
    - apps/web/src/app/(dashboard)/overview/page.tsx (the established getSession() + redirect usage to mirror)
    - .planning/phases/05-dashboard/05-RESEARCH.md (§Pattern 5 — page-level redirect, middleware unchanged)
  </read_first>
  <action>
    Convert `Home` to an async server component. At the very top of the component body, call `const session = await getSession();` (import getSession from '@/server/auth') and `if (session) redirect('/overview');` (import redirect from 'next/navigation'). Otherwise render the existing marketing markup unchanged below the guard. Do NOT modify middleware.ts — `/` stays a PUBLIC_ROUTE; the redirect is page-level only (RESEARCH Pattern 5). Keep all existing marketing markup and the SPLIT/STEPS data intact.
  </action>
  <verify>
    <automated>cd apps/web && grep -c "redirect('/overview')" "src/app/page.tsx"; git diff --quiet src/middleware.ts && echo "MIDDLEWARE-UNCHANGED"; npx tsc --noEmit -p tsconfig.json 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "redirect('/overview')" apps/web/src/app/page.tsx` returns 1.
    - page.tsx imports `redirect` from 'next/navigation' and `getSession` from '@/server/auth'; `Home` is now `async`.
    - apps/web/src/middleware.ts is unchanged (git diff is empty for that file).
    - Existing marketing content is still present (the redirect is added above it, not a replacement) — `grep -c "SPLIT" apps/web/src/app/page.tsx` >= 1.
    - `cd apps/web && npx tsc --noEmit` and `npm run lint` pass.
  </acceptance_criteria>
  <done>Authenticated users hitting / are redirected to /overview; unauthenticated visitors still see the marketing page; middleware untouched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                    | Description                                                                                                             |
| --------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| browser → Next RSC (root /) | Presence of the httpOnly access_token cookie (via getSession) determines redirect; never trusts client-claimed identity |

## STRIDE Threat Register

| Threat ID | Category               | Component          | Disposition | Mitigation Plan                                                                                                                                                               |
| --------- | ---------------------- | ------------------ | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-10   | Spoofing               | Root redirect gate | mitigate    | Redirect keys off getSession() (httpOnly access_token cookie) only; the JWT is still validated by the API/middleware on the destination route — the redirect grants no access |
| T-05-11   | Information Disclosure | Marketing leakage  | accept      | An attacker forging a cookie sees only a redirect to /overview, which is itself guarded by middleware + API auth; no data is exposed by the redirect decision                 |

</threat_model>

<verification>
- `cd apps/web && npx tsc --noEmit` and `npm run lint` — clean.
- Manual (human-verify): logged in, visit / → lands on /overview; logged out, visit / → marketing page renders.
</verification>

<success_criteria>

- Authenticated / redirects to /overview; marketing page preserved for logged-out users.
- middleware.ts unchanged; no new dependencies.
  </success_criteria>

<output>
Create `.planning/phases/05-dashboard/05-06-SUMMARY.md` when done.
</output>
</content>
