---
phase: 4
slug: wallets
status: draft
shadcn_initialized: true
preset: none
created: 2026-06-06
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for Phase 4: Wallets.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property          | Value                                                           |
| ----------------- | --------------------------------------------------------------- |
| Tool              | shadcn/ui                                                       |
| Preset            | not applicable (components.json present, no preset string used) |
| Style             | new-york                                                        |
| Component library | radix                                                           |
| Icon library      | lucide-react 1.8.0                                              |
| Font              | system-ui (Tailwind default)                                    |
| Tailwind version  | v4 (CSS-variable tokens via globals.css)                        |

**Source:** `apps/web/components.json` + `npx shadcn info` output. Identical to Phase 3 baseline.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage                                                                         |
| ----- | ----- | ----------------------------------------------------------------------------- |
| xs    | 4px   | Icon gaps, color swatch gaps, inline badge padding, wallet type indicator dot |
| sm    | 8px   | Compact field labels, balance breakdown row padding, card action spacing      |
| md    | 16px  | Default element spacing, form field gap, card padding supplement              |
| lg    | 24px  | Card internal padding (shadcn Card default), section gap, dialog body padding |
| xl    | 32px  | Layout column gap, page section vertical gap                                  |
| 2xl   | 48px  | Page top padding, major section breaks                                        |
| 3xl   | 64px  | Full-page layout spacing                                                      |

Exceptions:

- Color swatch palette grid: 6px gap between swatches (same exception as Phase 3; acceptable between xs and sm).
- Touch targets for icon-only action buttons (delete, restore, edit on transaction rows): minimum 44px height via padding on mobile.
- Transaction row height: no fixed height; natural flow with `py-3` (12px) gives approximately 44px touch target at body font size.

**Source:** Phase 3 UI-SPEC spacing contract (inherited); shadcn Card default padding (24px); existing LoginForm gap-4 (16px) pattern.

---

## Typography

| Role    | Size | Weight         | Line Height | Usage                                                                        |
| ------- | ---- | -------------- | ----------- | ---------------------------------------------------------------------------- |
| Body    | 14px | 400 (regular)  | 1.5         | Transaction descriptions, category names, mapping section body text          |
| Label   | 14px | 500 (medium)   | 1.4         | Form field labels, balance breakdown row labels, section subheadings         |
| Heading | 20px | 600 (semibold) | 1.2         | Page heading ("Wallets"), wallet detail heading (wallet name), dialog titles |
| Display | 28px | 600 (semibold) | 1.1         | Total wallet balance on the wallet card and detail page header               |

Notes:

- Negative balance amounts use the Display role typographically; only the color changes (destructive red).
- Amount inputs in transaction dialogs: 16px / weight 400 / line-height 1.5 (Body-class rendered inside `<Input>`).
- Soft-deleted transaction rows: same typography; `line-through` decoration and `opacity-50` applied via Tailwind class.
- Shadcn `CardTitle` renders `font-semibold leading-none` — aligns with Heading at 20px.

**Source:** Phase 3 UI-SPEC typography contract (inherited); consistent with existing shadcn new-york defaults and auth form patterns.

---

## Color

| Role            | Value                     | Usage                                                                                               |
| --------------- | ------------------------- | --------------------------------------------------------------------------------------------------- |
| Dominant (60%)  | `hsl(var(--background))`  | Page background, page-level surface                                                                 |
| Secondary (30%) | `hsl(var(--card))`        | Wallet cards, balance breakdown disclosure panel, dialog surface, transaction history rows          |
| Accent (10%)    | `hsl(var(--primary))`     | "Create Wallet" primary CTA, "Add Deposit" / "Add Withdrawal" buttons, active pagination button     |
| Destructive     | `hsl(var(--destructive))` | Negative balance display, delete wallet confirmation button, delete transaction confirmation button |

Accent reserved for:

- "Create Wallet" primary action button (wallet list page)
- "Add Deposit" and "Add Withdrawal" action buttons (wallet detail page)
- "Save" button in transaction dialog (create/edit)
- Active/current page button in pagination controls
- Focused input ring (Tailwind focus-visible, via shadcn Input default)

Destructive color reserved for:

- Negative balance amount display (text only; no button)
- "Delete Wallet" button inside the impact-detailing confirm dialog (`variant="destructive"`)
- "Delete" button inside the transaction soft-delete confirmation dialog (`variant="destructive"`)

Wallet color accents (per-wallet user-chosen color, stored as hex string per D-15):

These are data-driven colors, not design system tokens. They appear ONLY as:

- 4px left border accent on wallet cards (same pattern as Phase 3 account cards)
- 12px filled circle swatch in the color picker within the wallet form

Preset palette (8 swatches, D-15 — legible on light backgrounds, includes reference default `#10b981`):

| Swatch | Hex       | Name    |
| ------ | --------- | ------- |
| 1      | `#10b981` | Emerald |
| 2      | `#8b5cf6` | Violet  |
| 3      | `#f59e0b` | Amber   |
| 4      | `#f43f5e` | Rose    |
| 5      | `#3b82f6` | Blue    |
| 6      | `#ec4899` | Pink    |
| 7      | `#14b8a6` | Teal    |
| 8      | `#f97316` | Orange  |

**Source:** `apps/web/src/components/ui/button.tsx` (variant tokens); `card.tsx` (`bg-card`); CONTEXT.md D-13 (negative balance red), D-15 (preset palette); Phase 3 UI-SPEC color contract (palette inherited).

---

## Component Inventory

Components to install from shadcn official registry (no third-party):

| Component     | Command                        | Used In                                                                                          |
| ------------- | ------------------------------ | ------------------------------------------------------------------------------------------------ |
| dialog        | `npx shadcn add dialog`        | Add/edit transaction dialog (D-11), delete transaction confirmation (D-12), delete wallet (D-16) |
| badge         | `npx shadcn add badge`         | Wallet type indicator (PROFIT_FIRST / BLANK), transaction type indicator (DEPOSIT / WITHDRAWAL)  |
| dropdown-menu | `npx shadcn add dropdown-menu` | Per-wallet edit/delete actions on wallet card (⋮ menu)                                           |
| collapsible   | `npx shadcn add collapsible`   | Balance breakdown disclosure on wallet detail page (D-02)                                        |
| separator     | `npx shadcn add separator`     | Section dividers in wallet form (between income mapping / expense mapping / color sections)      |
| command       | `npx shadcn add command`       | Searchable multiselect for category mapping pickers (cmdk wrapper — D-05, D-07)                  |
| popover       | `npx shadcn add popover`       | Popover container for searchable category picker (pairs with command)                            |
| checkbox      | `npx shadcn add checkbox`      | Category item in the searchable multiselect picker                                               |
| radio-group   | `npx shadcn add radio-group`   | 3-mode expense selector (NONE / ALL / CATEGORIES — D-07)                                         |
| select        | `npx shadcn add select`        | Wallet type selector (PROFIT_FIRST / BLANK) in create form; PF account selector                  |

Already installed (from Phase 1): `button`, `card`, `input`, `label`.

Phase 3 adds (will be available by Phase 4 execution): `dialog`, `select`, `badge`, `separator`, `sheet`, `checkbox`, `dropdown-menu`, `progress`, `tooltip`.

Phase 4 net-new additions: `collapsible`, `command`, `popover`, `radio-group`.

**Note:** `command` and `popover` back the searchable multiselect for category pickers. The `cmdk` package is already pinned (1.1.1) and is a peer dependency of shadcn's `command` component — no new dependencies required.

**Source:** CONTEXT.md D-02 (collapsible breakdown), D-05/D-07 (category pickers), D-07 (3-mode selector), D-11 (transaction dialog), D-12/D-16 (confirmation dialogs); RESEARCH.md Standard Stack (cmdk already pinned).

---

## Layout Patterns

### Page Layout: `/wallets`

```
┌─────────────────────────────────────────────────────────┐
│  Wallets                              [+ Create Wallet]  │
│  ──────────────────────────────────────────────────────  │
│  Wallet cards grid (2-col on md+, 1-col on mobile)       │
│  ┌─────────────────────┐  ┌─────────────────────┐        │
│  │ [color-bar] Profit  │  │ [color-bar] Owner   │        │
│  │ PROFIT_FIRST        │  │ Pay PROFIT_FIRST    │        │
│  │ ₱X,XXX.XX           │  │ ₱X,XXX.XX           │   [⋮]  │
│  └─────────────────────┘  └─────────────────────┘        │
│  … (more wallets, ordered by sortOrder)                  │
└─────────────────────────────────────────────────────────┘
```

- Page is an RSC (`app/(dashboard)/wallets/page.tsx`); fetches wallet list server-side.
- No inline filters — the list is not filterable (filter is per-detail, not per-list). D-03 ordering only.
- Cards grid: `grid grid-cols-1 md:grid-cols-2 gap-6`.

### Wallet Card

```
┌──────────────────────────────────────────────────┐
│ [4px color bar] Wallet Name         [PROFIT_FIRST] [⋮] │
│                 ₱X,XXX.XX  total balance                │
│                 (negative: ₱-X,XXX.XX in red)           │
└──────────────────────────────────────────────────┘
```

- Color bar: 4px left border in wallet's `color` hex (same pattern as Phase 3 account cards).
- Badge: `PROFIT_FIRST` or `BLANK` in `variant="secondary"`.
- Total balance: Display role (28px semibold). Negative balance uses `text-destructive`.
- Dropdown [⋮]: "Edit" → navigates to `/wallets/[id]/edit` (or opens edit form on detail); "Delete" → opens impact-detailing confirmation dialog (D-16).
- Card is a link to `/wallets/[walletId]`.

### Empty State (Wallet List)

```
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  No wallets yet                                              │
│  Track your Profit First allocations by creating a wallet    │
│  for each account.                                           │
│                                                              │
│  Quick-create for unlinked accounts (D-04):                  │
│  [+ Profit]  [+ Owner Pay]  [+ Tax]  [+ Op. Expenses]        │
│                                                              │
│                         [+ Create Wallet]                    │
└──────────────────────────────────────────────────────────────┘
```

- Quick-create buttons are `variant="outline"` size="sm"; each navigates to `/wallets/new` with `?pfAccountId={id}` pre-filled (standard create path, D-04).
- Only unlinked PF accounts are shown as quick-create suggestions.
- "Create Wallet" button is the primary accent CTA.

### Create Wallet Form: `/wallets/new`

```
┌───────────────────────────────────────────────────────────┐
│  Create Wallet                                            │
│  ──────────────────────────────────────────────────────── │
│  Wallet Name                                              │
│  [_________________________________]                      │
│                                                           │
│  Wallet Type                                              │
│  [Profit First ▼]  (or BLANK)                             │
│                                                           │
│  Linked Account (shown only when type = PROFIT_FIRST)     │
│  [Select allocation account ▼]                            │
│                                                           │
│  Color                                                    │
│  [●][●][●][●][●][●][●][●]  (8 preset swatches)            │
│                                                           │
│  ── Income Categories ─────────────────────────────────── │
│  (Section hidden when type = PROFIT_FIRST — D-08)         │
│  [Search and select categories…]  (cmdk multiselect)      │
│                                                           │
│  ── Expense Settings ──────────────────────────────────── │
│  (●) No expenses                                          │
│  (○) Auto-deduct all expenses                             │
│  (○) Specific expense categories                          │
│      [Search and select categories…]  (shown when ○ above) │
│                                                           │
│                           [Cancel]  [Create Wallet]       │
└───────────────────────────────────────────────────────────┘
```

- Form is a client component (`NewWalletForm.tsx`).
- Type selector is a `<Select>` with options: "Profit First" / "Standalone".
- Linked Account selector appears only when type = PROFIT_FIRST; populates from PF accounts without an existing wallet link.
- Income Categories section is conditionally hidden when type = PROFIT_FIRST (D-08).
- Expense mode uses RadioGroup with 3 options (D-07); "Specific categories" picker conditionally revealed.
- Cancel navigates back to `/wallets`.

### Wallet Detail Page: `/wallets/[walletId]`

```
┌──────────────────────────────────────────────────────────────┐
│  [← Back]  Wallet Name          [Edit]  [Delete]             │
│  ────────────────────────────────────────────────────────── │
│  Total Balance                                               │
│  ₱X,XXX.XX  (negative: red)      [PROFIT_FIRST badge]        │
│                                                              │
│  ▶ Balance Breakdown  (collapsible — D-02)                   │
│  ┌──────────────────────────────────────────────────────┐    │
│  │ PF Allocation       ₱X,XXX.XX                        │    │
│  │ Mapped Income       ₱X,XXX.XX   (hidden if 0)        │    │
│  │ Mapped Expenses     ₱X,XXX.XX   (hidden if 0)        │    │
│  │ Manual Deposits     ₱X,XXX.XX   (hidden if 0)        │    │
│  │ Manual Withdrawals  ₱X,XXX.XX   (hidden if 0)        │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                              │
│  ────────────────────────────────────────────────────────── │
│  Transaction History           [+ Add Deposit] [+ Add Withdrawal] │
│                                                              │
│  (manual-transaction blocking message if blocked — D-11/context) │
│                                                              │
│  Date        Description          Amount       Type   [actions] │
│  2026-06-01  Deposit description  ₱X,XXX.XX    DEP    [✎][🗑]   │
│  ~~2026-05-30  Deleted txn       ~~₱X,XXX.XX   ~~WD~~ [Restore] │
│  2026-05-20  Income: Consulting   ₱X,XXX.XX    INC    (read-only) │
│  2026-05-15  Expense: Rent       -₱X,XXX.XX    EXP    (read-only) │
│  ────────────────────────────────────────────────────────── │
│                [← Prev]  Page 1 of N  [Next →]               │
└──────────────────────────────────────────────────────────────┘
```

- Balance breakdown collapsible is closed by default.
- Zero-value breakdown rows are hidden (D-02).
- Soft-deleted transaction rows: `opacity-50 line-through` on text; single "Restore" button (`variant="outline"` size="sm") replaces edit/delete actions (D-09).
- Auto-sourced transactions (income, expenses) are read-only — no edit/delete actions shown.
- Manual transactions show edit (pencil icon) and delete (trash icon) action buttons.
- Transaction type column shows abbreviated badge: "DEP" / "WD" for manual; "INC" / "EXP" for auto-sourced.
- Pagination: page-based controls at bottom using nuqs for URL `page` param (D-10).
- "Add Deposit" and "Add Withdrawal" buttons are disabled and show a tooltip with the blocking message when `assertCanInsertTransaction` would reject them (D-11).

### Add / Edit Transaction Dialog

```
┌──────────────────────────────┐
│  Add Deposit          [✕]    │
│  ────────────────────────── │
│  Amount (₱)                  │
│  [___________________]       │
│                              │
│  Date                        │
│  [___________________]       │
│                              │
│  Description (optional)      │
│  [___________________]       │
│                              │
│           [Cancel]  [Save]   │
└──────────────────────────────┘
```

- Dialog title: "Add Deposit" / "Add Withdrawal" / "Edit Deposit" / "Edit Withdrawal".
- Amount field: number input, minimum 0.01, accepts decimal (converts to cents on submit).
- Date field: date input (YYYY-MM-DD), defaults to today.
- Save button uses `variant="default"` (accent).

### Delete Transaction Confirmation Dialog

```
┌──────────────────────────────────────┐
│  Delete Transaction           [✕]    │
│  ──────────────────────────────────  │
│  Delete this ₱X,XXX.XX deposit?      │
│  You can restore it from the list.   │
│                                      │
│           [Cancel]  [Delete]         │
└──────────────────────────────────────┘
```

- "Delete" button uses `variant="destructive"`.
- A success toast fires after deletion: "Transaction deleted." with an undo hint not required (restore is inline per D-09).

### Delete Wallet Confirmation Dialog (D-16)

```
┌────────────────────────────────────────────┐
│  Delete Wallet                      [✕]    │
│  ──────────────────────────────────────── │
│  Delete "Wallet Name"?                     │
│  This will permanently remove:             │
│  • N manual transactions                   │
│  • Income and expense category mappings    │
│  This cannot be undone.                    │
│                                            │
│              [Cancel]  [Delete Wallet]     │
└────────────────────────────────────────────┘
```

- "Delete Wallet" button uses `variant="destructive"`.
- Impact counts are fetched from the API before showing the dialog (or passed as props from the list response).
- Dialog copy is populated with the actual wallet name and transaction count.

---

## Interaction States

| Element                        | State                        | Visual Treatment                                                                         |
| ------------------------------ | ---------------------------- | ---------------------------------------------------------------------------------------- |
| Wallet card                    | Default                      | `bg-card`, `shadow-sm`, `rounded-xl`, 4px left color border                              |
| Wallet card                    | Hover                        | `shadow-md` transition (subtle elevation)                                                |
| Wallet total balance           | Positive                     | Default foreground color, Display role                                                   |
| Wallet total balance           | Negative (D-13)              | `text-destructive`, Display role                                                         |
| Balance breakdown row          | Zero value                   | Hidden entirely (D-02)                                                                   |
| Balance breakdown disclosure   | Closed (default)             | Shows chevron-right icon, "Balance Breakdown" label                                      |
| Balance breakdown disclosure   | Open                         | Shows chevron-down icon, breakdown rows animate in                                       |
| Transaction row — manual       | Default                      | Normal opacity, edit + delete icons visible on hover                                     |
| Transaction row — soft-deleted | Soft-deleted (D-09)          | `opacity-50 line-through` on all text; "Restore" button replaces edit/delete             |
| Transaction row — auto-sourced | Read-only                    | No action icons; italic or muted type badge to signal auto-sourced origin                |
| Category in picker             | Available                    | Normal, selectable                                                                       |
| Category in picker             | Already mapped elsewhere     | `disabled` styling — greyed out, `cursor-not-allowed`, tooltip "Mapped to {wallet name}" |
| Add Deposit button             | Blocked (PF wallet / mapped) | `disabled`, tooltip shows blocking reason message (D-11 reference copy)                  |
| Add Withdrawal button          | Blocked (expense-mapped)     | `disabled`, tooltip shows blocking reason message                                        |
| Expense mode — SPECIFIC        | Selected                     | Category picker revealed below radio group                                               |
| Expense mode — SPECIFIC        | Zero categories selected     | Validation error "Select at least one expense category"                                  |
| Create Wallet button           | Submitting                   | `disabled` + spinner, text "Creating…"                                                   |
| Save transaction button        | Submitting                   | `disabled` + spinner, text "Saving…"                                                     |
| Pagination — current page      | Active                       | `variant="default"` button (accent fill)                                                 |
| Pagination — other pages       | Default                      | `variant="outline"` button                                                               |
| Color swatch                   | Selected                     | Ring indicator (`ring-2 ring-offset-2 ring-primary`)                                     |
| Color swatch                   | Unselected                   | No ring                                                                                  |

---

## Copywriting Contract

| Element                                      | Copy                                                                                                                 |
| -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| Page heading                                 | "Wallets"                                                                                                            |
| Page sub-heading                             | "Manage your Profit First wallets and track money across accounts."                                                  |
| Primary CTA — create wallet                  | "Create Wallet"                                                                                                      |
| Empty state heading                          | "No wallets yet"                                                                                                     |
| Empty state body                             | "Track your Profit First allocations by creating a wallet for each account."                                         |
| Empty state quick-create prefix              | "Quick-create for your allocation accounts:"                                                                         |
| Form heading — create                        | "Create Wallet"                                                                                                      |
| Form heading — edit                          | "Edit Wallet"                                                                                                        |
| Wallet type option — PROFIT_FIRST            | "Profit First"                                                                                                       |
| Wallet type option — BLANK                   | "Standalone"                                                                                                         |
| Wallet type description — PROFIT_FIRST       | "Funded by your Profit First allocation percentage."                                                                 |
| Wallet type description — BLANK              | "Manually managed wallet with no automatic allocation."                                                              |
| Linked account field label                   | "Allocation Account"                                                                                                 |
| Income categories section heading            | "Income Categories"                                                                                                  |
| Income categories description                | "Income from these categories will automatically credit this wallet."                                                |
| Expense mode heading                         | "Expense Settings"                                                                                                   |
| Expense mode — NONE label                    | "No automatic expenses"                                                                                              |
| Expense mode — ALL label                     | "Auto-deduct all expenses"                                                                                           |
| Expense mode — CATEGORIES label              | "Specific expense categories"                                                                                        |
| Expense categories field label               | "Expense Categories"                                                                                                 |
| Balance breakdown section label              | "Balance Breakdown"                                                                                                  |
| Balance row — PF allocation                  | "Profit First Allocation"                                                                                            |
| Balance row — mapped income                  | "Mapped Income"                                                                                                      |
| Balance row — mapped expenses                | "Mapped Expenses"                                                                                                    |
| Balance row — manual deposits                | "Deposits"                                                                                                           |
| Balance row — manual withdrawals             | "Withdrawals"                                                                                                        |
| Transaction history heading                  | "Transaction History"                                                                                                |
| Add deposit button                           | "Add Deposit"                                                                                                        |
| Add withdrawal button                        | "Add Withdrawal"                                                                                                     |
| Dialog title — add deposit                   | "Add Deposit"                                                                                                        |
| Dialog title — add withdrawal                | "Add Withdrawal"                                                                                                     |
| Dialog title — edit deposit                  | "Edit Deposit"                                                                                                       |
| Dialog title — edit withdrawal               | "Edit Withdrawal"                                                                                                    |
| Dialog title — delete transaction            | "Delete Transaction"                                                                                                 |
| Dialog body — delete transaction             | "Delete this {amount} {type}? You can restore it from the list."                                                     |
| Dialog title — delete wallet                 | "Delete Wallet"                                                                                                      |
| Dialog body — delete wallet                  | "Delete \"{name}\"? This will permanently remove {N} transactions and all category mappings. This cannot be undone." |
| Transaction field — amount label             | "Amount"                                                                                                             |
| Transaction field — date label               | "Date"                                                                                                               |
| Transaction field — description label        | "Description"                                                                                                        |
| Transaction field — description placeholder  | "Optional note"                                                                                                      |
| Restore button label                         | "Restore"                                                                                                            |
| Transaction type badge — DEPOSIT             | "Deposit"                                                                                                            |
| Transaction type badge — WITHDRAWAL          | "Withdrawal"                                                                                                         |
| Transaction type badge — auto income         | "Income"                                                                                                             |
| Transaction type badge — auto expense        | "Expense"                                                                                                            |
| Blocking message — PF wallet deposit         | "Profit First wallets do not accept manual deposits — they derive their allocation from received income."            |
| Blocking message — income-mapped deposit     | "This wallet auto-credits matching income. Manual deposits would double-count — record the income instead."          |
| Blocking message — expense-mapped withdrawal | "This wallet auto-deducts matching expenses. Manual withdrawals would double-count — record an expense instead."     |
| Category conflict tooltip                    | "Already mapped to {wallet name} — remove it there first."                                                           |
| Validation — SPECIFIC mode no categories     | "Select at least one expense category."                                                                              |
| Validation — PF wallet requires account      | "Select a Profit First allocation account."                                                                          |
| Validation — amount minimum                  | "Enter an amount greater than zero."                                                                                 |
| Error — wallet name taken                    | "A wallet with this name already exists."                                                                            |
| Error — PF account already linked            | "This allocation account already has a wallet. Choose a different account."                                          |
| Error — category already mapped (409)        | "Already mapped to another wallet — remove it there first."                                                          |
| Error — generic server error                 | "Something went wrong. Please try again."                                                                            |
| Success toast — wallet created               | "Wallet created."                                                                                                    |
| Success toast — wallet updated               | "Wallet updated."                                                                                                    |
| Success toast — wallet deleted               | "Wallet deleted."                                                                                                    |
| Success toast — transaction created          | "Transaction saved."                                                                                                 |
| Success toast — transaction updated          | "Transaction updated."                                                                                               |
| Success toast — transaction deleted          | "Transaction deleted."                                                                                               |
| Success toast — transaction restored         | "Transaction restored."                                                                                              |

**Source:** CONTEXT.md D-04 (empty state suggestions), D-06 (conflict blocked copy), D-07 (3-mode selector), D-09 (inline restore), D-11 (blocking messages), D-12 (delete confirmation), D-16 (impact-detailing delete); RESEARCH.md Pattern 3 (`assertCanInsertTransaction` blocking copy ported verbatim from reference); Phase 3 UI-SPEC (toast and error copy patterns inherited).

Destructive actions in this phase:

- **Delete manual transaction** (D-12): confirmation via Dialog; soft-delete (restorable inline); "Delete" button `variant="destructive"`.
- **Delete wallet** (D-16): confirmation via Dialog with impact detail (N transactions + mappings); hard-delete cascades; "Delete Wallet" button `variant="destructive"`; cannot be undone.

---

## Registry Safety

| Registry        | Blocks Used                                                                                           | Safety Gate                      |
| --------------- | ----------------------------------------------------------------------------------------------------- | -------------------------------- |
| shadcn official | dialog, badge, dropdown-menu, collapsible, separator, command, popover, checkbox, radio-group, select | not required — official registry |

No third-party registries declared for this phase.

Phase 4 net-new installs from official registry: `collapsible`, `command`, `popover`, `radio-group`.
Phase 3 components reused (already installed by Phase 3): `dialog`, `select`, `badge`, `separator`, `checkbox`, `dropdown-menu`.

**Source:** Component inventory derived from CONTEXT.md D-02/D-05/D-07/D-11/D-12/D-16 and RESEARCH.md Standard Stack. All components are from the shadcn official registry (`https://ui.shadcn.com/r/styles/{style}/{name}.json`).

---

## Pre-Population Sources

| Source          | Decisions Used                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CONTEXT.md      | D-01 (card grid), D-02 (collapsible breakdown), D-03 (sortOrder), D-04 (empty state PF suggestions), D-05 (inline category mapping), D-06 (conflict blocking), D-07 (3-mode selector), D-08 (PF hides income section), D-09 (inline soft-delete restore), D-10 (page pagination + nuqs), D-11 (transaction dialog), D-12 (delete confirmation), D-13 (negative balance red), D-14 (formatCurrency PHP default), D-15 (8-swatch palette), D-16 (impact-detailing delete) — 16 decisions |
| RESEARCH.md     | Standard stack (lucide-react icons, cmdk, nuqs, sonner, date-fns), Architecture patterns (layout structure, BFF proxy, server components), blocking copy from `assertCanInsertTransaction`, balance breakdown row labels, formatCurrency — 10 items                                                                                                                                                                                                                                    |
| components.json | Style (new-york), base (radix), tailwind v4 CSS vars, installed components (button/card/input/label)                                                                                                                                                                                                                                                                                                                                                                                   |
| Phase 3 UI-SPEC | Typography contract, spacing contract, color palette (8 swatches), shadcn design system baseline, toast/error patterns — all inherited                                                                                                                                                                                                                                                                                                                                                 |
| User input      | 0 (all decisions derived from upstream artifacts + existing design system)                                                                                                                                                                                                                                                                                                                                                                                                             |

---

## Assumptions (auto mode)

| #   | Assumption                                                                                                                                                                                                            | Impact if Wrong                                                                                     |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| A1  | Phase 3 installs shadcn `dialog`, `select`, `badge`, `separator`, `checkbox`, `dropdown-menu` before Phase 4 executes; Phase 4 only needs `collapsible`, `command`, `popover`, `radio-group` as net-new               | Phase 4 executor must install all listed components if Phase 3 hasn't run yet                       |
| A2  | A `(dashboard)/layout.tsx` exists after Phase 2 or 3 with sidebar nav + auth guard; the `/wallets` route nests inside it                                                                                              | Phase 4 planner must add a minimal dashboard layout shell if not yet established                    |
| A3  | Page size for transaction pagination defaults to 20 items per page (from RESEARCH.md walletTransactionQuerySchema default); Phase 2 pagination page size should align                                                 | Adjust `size` default if Phase 2 establishes a different standard (e.g. 25)                         |
| A4  | Transaction type badges use `variant="secondary"` to avoid conflict with the per-wallet color accent on the left border                                                                                               | Swap to `variant="outline"` if secondary is too muted; avoid `variant="default"` (accent collision) |
| A5  | The `(dashboard)/wallets/` route group matches the recommended structure in RESEARCH.md; if Phase 2 establishes a different dashboard route group name, adjust accordingly                                            | Update route paths to match whatever group name Phase 2 establishes                                 |
| A6  | `lucide-react` icons used: `ArrowDownLeft` (deposit), `ArrowUpRight` (withdrawal), `Trash2` (delete), `RotateCcw` (restore), `Pencil` (edit), `ChevronRight`/`ChevronDown` (collapsible), `MoreVertical` (⋮ dropdown) | If icon names differ in v1.8.0, substitute nearest equivalent                                       |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
