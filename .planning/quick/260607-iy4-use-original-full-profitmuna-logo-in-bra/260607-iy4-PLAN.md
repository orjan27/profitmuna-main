---
phase: quick-260607-iy4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/BrandMark.tsx
  - apps/web/src/components/DashboardNav.tsx
  - apps/web/src/app/(auth)/login/page.tsx
  - apps/web/src/app/(auth)/register/page.tsx
  - apps/web/src/app/(auth)/verify-email/page.tsx
  - apps/web/src/app/(auth)/forgot-password/page.tsx
  - apps/web/src/app/(auth)/reset-password/page.tsx
  - apps/web/public/profitmuna-mark.webp
autonomous: true
requirements: [QUICK-260607-iy4]

must_haves:
  truths:
    - 'BrandMark renders the original full ProfitMuna logo image (/profitmuna-logo.webp), not a cropped mark + typeset text'
    - 'All 7 call sites compile against the new prop surface'
    - 'The unused profitmuna-mark.webp is deleted and unreferenced'
    - 'apps/web typecheck passes'
  artifacts:
    - path: 'apps/web/src/components/BrandMark.tsx'
      provides: 'Single-image brand lockup using /profitmuna-logo.webp'
      contains: 'profitmuna-logo.webp'
  key_links:
    - from: 'apps/web/src/components/BrandMark.tsx'
      to: '/profitmuna-logo.webp'
      via: 'next/image src'
      pattern: "profitmuna-logo\\.webp"
---

<objective>
Replace the BrandMark lockup (cropped glyph mark + typeset "Profitmuna" span) with the
original full ProfitMuna logo image rendered as-is. Drop the typeset text and the now-dead
`withWordmark` / `wordmarkClassName` props. Update all 7 call sites and delete the orphaned
cropped-mark asset.

Purpose: The original full logo (stacked glyph + navy "PROFITMUNA" wordmark) is the correct
brand identity. The current component crops it and re-typesets the name with wrong casing.
Output: Simplified BrandMark.tsx, updated call sites, deleted profitmuna-mark.webp.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md

<facts>
- /profitmuna-logo.webp intrinsic dimensions are 189 x 94 (verified via WebP header parse — VP8X). Use width={189} height={94} on the Image.
- The logo is a stacked vertical lockup (glyph above navy "PROFITMUNA" wordmark), taller than the old h-[18px] glyph-only crop.
- References to `withWordmark`, `wordmarkClassName`, and `profitmuna-mark.webp` exist ONLY in BrandMark.tsx (verified via grep). No external code reads those props.
- Call sites: DashboardNav.tsx:50 uses bare `<BrandMark />`; the 6 auth-page usages pass `markClassName="h-6"` (login:10, register:10, verify-email:16, forgot-password:10, reset-password:17 and :38).
</facts>

<current_component>
Current BrandMark.tsx props: `withWordmark` (bool), `className`, `markClassName`, `wordmarkClassName`.
Renders an inline-flex span: cropped Image (/profitmuna-mark.webp, 108x37, h-[18px]) + conditional
typeset span ("Profitmuna", text-ink). JSDoc references cropping and the "One Sans Rule" — now obsolete.
</current_component>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite BrandMark to render the full logo image</name>
  <files>apps/web/src/components/BrandMark.tsx</files>
  <action>
    Rewrite BrandMark.tsx so it renders ONLY a next/image of the full original logo.
    Keep the `Image` import and the `cn` import from '@/lib/utils'.
    New props interface (BrandMarkProps): keep `className?: string` (applies to the Image wrapper or
    the Image itself — render a single Image, so apply className directly to the Image's class chain)
    and keep `markClassName?: string` as the size-override prop (preserves the existing call-site
    surface that passes `markClassName="h-6"`, minimizing churn). REMOVE `withWordmark` and
    `wordmarkClassName` entirely.
    Render a single Image: src="/profitmuna-logo.webp", alt="ProfitMuna" (real alt text — this is the
    brand name, no longer aria-hidden decorative crop), width={189}, height={94}, and
    className={cn('h-9 w-auto', markClassName, className)} as the default sizing (h-9 = 36px is a
    sensible default height for a stacked lockup; call sites override via markClassName as needed).
    Do NOT wrap in an inline-flex span and do NOT render any typeset text span. Return the Image directly.
    Replace the obsolete JSDoc block: write a short comment describing the component as rendering the
    full ProfitMuna brand logo from public/profitmuna-logo.webp. Remove all language about cropping,
    the pastel pill glyph, navy unreadability, and the "One Sans Rule".
    Keep the explicit `: React.JSX.Element` return type and named export (no default export — shared lib code per typescript rule).
  </action>
  <verify>
    <automated>cd apps/web && npx tsc --noEmit 2>&1 | tail -5; grep -c "profitmuna-logo.webp" src/components/BrandMark.tsx; grep -v '^\s*\*' src/components/BrandMark.tsx | grep -c -E "withWordmark|wordmarkClassName|profitmuna-mark|Profitmuna<|>Profitmuna"</automated>
  </verify>
  <done>BrandMark.tsx renders one Image of /profitmuna-logo.webp (189x94), no typeset span, no withWordmark/wordmarkClassName props; grep count for profitmuna-logo.webp is 1 and count for the removed tokens is 0; tsc reports no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Update call sites and pick sensible heights</name>
  <files>apps/web/src/components/DashboardNav.tsx, apps/web/src/app/(auth)/login/page.tsx, apps/web/src/app/(auth)/register/page.tsx, apps/web/src/app/(auth)/verify-email/page.tsx, apps/web/src/app/(auth)/forgot-password/page.tsx, apps/web/src/app/(auth)/reset-password/page.tsx</files>
  <action>
    The logo is now a taller stacked lockup, so the old sizes are wrong.
    DashboardNav.tsx:50 — `<BrandMark />` currently relies on default size. The nav is a horizontal
    bar; a stacked lockup at the default h-9 may be too tall for a nav row. Set an explicit compact
    height for nav: `<BrandMark markClassName="h-8" />` (32px) so the stacked logo fits a nav bar
    without dominating it. (If the surrounding nav row height is clearly smaller, the executor may
    choose h-7; document the choice in the SUMMARY.)
    The 6 auth-page usages currently pass `markClassName="h-6"` (sized for the old glyph-only crop).
    Bump each to `markClassName="h-12"` (48px) so the full stacked lockup reads clearly as the page
    header brand on the auth screens. Update all 6 occurrences: login:10, register:10, verify-email:16,
    forgot-password:10, reset-password:17 and reset-password:38.
    Do not change any other markup or imports at the call sites.
  </action>
  <verify>
    <automated>cd apps/web && grep -rn "BrandMark" "src/app/(auth)" src/components/DashboardNav.tsx | grep -v "import"; npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>DashboardNav uses an explicit compact height (h-8 or h-7); all 6 auth-page BrandMark usages pass an enlarged height (h-12); apps/web typecheck passes with no errors.</done>
</task>

<task type="auto">
  <name>Task 3: Delete the orphaned cropped-mark asset</name>
  <files>apps/web/public/profitmuna-mark.webp</files>
  <action>
    Verify no remaining references to profitmuna-mark.webp anywhere in apps/web/src (it was only used
    by the old BrandMark, now removed in Task 1). If grep confirms zero references in source, delete
    apps/web/public/profitmuna-mark.webp. If ANY reference remains, do NOT delete — report the
    remaining reference instead and fix the reference first.
  </action>
  <verify>
    <automated>cd apps/web && if grep -rq "profitmuna-mark" src; then echo "FAIL: references remain"; grep -rn "profitmuna-mark" src; else rm -f public/profitmuna-mark.webp; test ! -f public/profitmuna-mark.webp && echo "DELETED OK"; fi</automated>
  </verify>
  <done>No source references to profitmuna-mark remain and public/profitmuna-mark.webp no longer exists.</done>
</task>

</tasks>

<verification>
- `cd apps/web && npx tsc --noEmit` passes (no new errors).
- `grep -rn "profitmuna-mark.webp\|withWordmark\|wordmarkClassName" apps/web/src` returns nothing.
- `grep -rn ">Profitmuna<\|Profitmuna<" apps/web/src/components/BrandMark.tsx` returns nothing (typeset text removed).
- `apps/web/public/profitmuna-mark.webp` does not exist.
- BrandMark renders /profitmuna-logo.webp via next/image with width=189 height=94.
</verification>

<success_criteria>

- BrandMark displays the original full ProfitMuna logo image as-is, no recolor, no typeset text.
- All 7 call sites compile and use sensible heights for the taller stacked lockup.
- Orphaned cropped-mark asset deleted; no dangling references.
- apps/web typecheck green.
  </success_criteria>

<output>
Create `.planning/quick/260607-iy4-use-original-full-profitmuna-logo-in-bra/260607-iy4-SUMMARY.md` when done.
</output>
