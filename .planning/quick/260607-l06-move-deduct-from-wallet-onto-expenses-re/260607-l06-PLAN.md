---
phase: quick-260607-l06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/db/src/schema.ts
  - packages/db/migrations/0006_*.sql
  - packages/db/migrations/0007_default_wallet_backfill.sql
  - packages/db/migrations/meta/_journal.json
  - apps/api/tests/helpers/db.ts
  - apps/api/src/schemas/expense.ts
  - apps/api/src/schemas/wallets.ts
  - apps/api/src/services/expense-service.ts
  - apps/api/src/services/wallet-service.ts
  - apps/api/src/services/dashboard-service.ts
  - apps/api/src/services/auth-service.ts
  - apps/api/tests/expense.test.ts
  - apps/api/tests/wallets.test.ts
  - apps/api/tests/dashboard.test.ts
  - apps/api/tests/auth.test.ts
  - apps/web/src/lib/constants.ts
  - apps/web/src/types/wallet.ts
  - apps/web/src/app/(dashboard)/expenses/page.tsx
  - apps/web/src/components/expense-form.tsx
  - apps/web/src/app/(dashboard)/expenses/new/_actions/create-expense.ts
  - apps/web/src/server/expense-actions.ts
  - apps/web/src/components/edit-expense-dialog.tsx
  - apps/web/src/components/expense-list.tsx
  - apps/web/src/components/RecordSheet.tsx
  - apps/web/src/server/record-sheet.ts
  - apps/web/src/components/NewWalletForm.tsx
  - apps/web/src/components/EditWalletDialog.tsx
  - apps/web/src/app/(dashboard)/wallets/new/page.tsx
  - apps/web/src/app/(dashboard)/wallets/[walletId]/page.tsx
  - apps/web/src/components/WalletRow.tsx
  - apps/web/src/server/wallet-actions.ts
autonomous: true
requirements: [QUICK-260607-L06]

must_haves:
  truths:
    - "Every new/edited expense requires a wallet via the 'Paid with' selector"
    - "Each user has an undeletable 'Default' wallet seeded at registration"
    - 'Wallet balances deduct expenses by walletId and may go negative'
    - 'Wallets soft-delete: gone from lists but their name still renders on past expenses'
    - 'payment_method and wallet-side Expense Settings are fully removed from shipping code'
    - 'Manual withdrawals are allowed on all wallets (DEPOSIT guard unchanged)'
  artifacts:
    - path: 'packages/db/migrations/0007_default_wallet_backfill.sql'
      provides: 'Hand-written Default wallet backfill, journal-registered'
    - path: 'apps/api/src/services/wallet-service.ts'
      provides: 'seedDefaultWallet, getExpensesByWalletCents, soft-delete remove'
      contains: 'seedDefaultWallet'
    - path: 'apps/api/src/services/expense-service.ts'
      provides: 'resolveWalletName, walletId/walletName persistence'
      contains: 'resolveWalletName'
  key_links:
    - from: 'apps/api/src/services/auth-service.ts'
      to: 'wallet-service seedDefaultWallet'
      via: 'register + Google new-user branch'
      pattern: 'seedDefaultWallet'
    - from: 'apps/web/src/components/expense-form.tsx'
      to: 'walletId payload'
      via: "required 'Paid with' wallet Select"
      pattern: 'walletId'
---

<objective>
Move expense-to-wallet deduction from a wallet-side "Expense Settings" model onto each expense explicitly. The optional `payment_method` field is repurposed into a required `wallet_id` selector ("Paid with"). Wallet-side expense mappings, the `wallet_expense_category_mappings` junction table, `auto_deduct_all_expenses`, and all supporting code are removed. Every user gets an undeletable "Default" wallet; wallets soft-delete; balances may go negative.

Purpose: Make deduction visible and explicit at expense entry instead of derived invisibly at balance-query time.
Output: DB schema + migrations, API schemas/services/routes, web expense flows + wallet UI cleanup, updated tests, repo-wide sweep clean.

This converts the user-APPROVED plan at /home/orjanbognot/.claude/plans/write-a-detailed-plan-merry-salamander.md into executable form. All decisions are LOCKED — do not redesign or revisit. Implement exactly as specified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@/home/orjanbognot/.claude/plans/write-a-detailed-plan-merry-salamander.md

# Project skills (consult before non-trivial stack code):

@.claude/skills/hono-best-practices/SKILL.md
@.claude/skills/nextjs-best-practices/SKILL.md

<locked_decisions>

- payment_method REPLACED entirely by wallet_id — column, enum, constants all dropped (no payment_method anywhere in shipping code)
- Existing expenses: NO backfill — legacy rows get wallet_id = NULL (deduct from nothing)
- Wallet on expense: REQUIRED for new/edited expenses; every user gets undeletable "Default" wallet; balances may go negative
- Wallet deletion: SOFT delete (deleted_at); expenses keep pointing at soft-deleted wallets; Default wallet cannot be deleted
- Withdrawal guard: DROPPED — manual withdrawals allowed on all wallets; DEPOSIT guard (PF-linked / income-mapped) UNCHANGED
- Denormalize wallet_name onto expenses (mirrors category_name); soft-deleted names render without join
- resolveWalletName(db, walletId, userId) mirrors resolveCategoryName: throws 400 invalid_wallet if not owned or soft-deleted
- Editing a legacy NULL-wallet expense: form preselects Default wallet
- On wallet soft delete: hard-delete its income-category mapping rows AND null profitFirstAccountId so both re-link freely
- Income-category mappings and PF-account linking are otherwise UNTOUCHED
- Default wallet: name 'Default', isDefault true, color '#10b981', sortOrder 0, profitFirstAccountId null
  </locked_decisions>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: DB schema + migrations + test DDL sync</name>
  <files>packages/db/src/schema.ts, packages/db/migrations/0006_*.sql, packages/db/migrations/0007_default_wallet_backfill.sql, packages/db/migrations/meta/_journal.json, apps/api/tests/helpers/db.ts</files>
  <action>
Implement Steps 1–3 of the approved plan.

Schema (packages/db/src/schema.ts):

- `expenses` table: remove `paymentMethod: text('payment_method')`. Add `walletId: integer('wallet_id').references(() => wallets.id)` (nullable, no cascade — lazy arrow ref is fine even though expenses is declared before wallets). Add `walletName: text('wallet_name')` (nullable). Add an index on `(userId, walletId)`.
- `wallets` table: remove `autoDeductAllExpenses`. Add `isDefault: integer('is_default', { mode: 'boolean' }).notNull().default(false)`. Add `deletedAt: text('deleted_at')` (nullable; null = active).
- Delete the `walletExpenseCategoryMappings` table and its indexes entirely. `walletIncomeCategoryMappings` stays.

Migrations:

- Run `cd packages/db && npx drizzle-kit generate` to produce `0006_*.sql`. INSPECT the generated SQL before applying. Expect plain ALTERs (D1 supports ADD/DROP COLUMN; migration 0004 already does a bare DROP COLUMN): expenses ADD wallet_id + ADD wallet_name + DROP payment_method + new index; wallets ADD is_default NOT NULL DEFAULT false + ADD deleted_at + DROP auto_deduct_all_expenses; DROP TABLE wallet_expense_category_mappings. HIGHEST RISK: if drizzle-kit emits a 12-step table-recreate for expenses (FK add), verify it preserves the category_id FK, ALL indexes, and the data copy before applying.
- Hand-write `0007_default_wallet_backfill.sql` with the INSERT...SELECT from the approved plan (seeds 'Default' wallet for every user lacking one). Register it in meta/\_journal.json with an entry matching the existing shape: `{idx, version: "6", when, tag, breakpoints: true}`.
- Apply locally: `cd apps/api && npx wrangler d1 migrations apply DB --local` (miniflare D1).

Test DDL sync (apps/api/tests/helpers/db.ts): the hand-maintained DDL constant must mirror the schema exactly — drop payment_method & auto_deduct_all_expenses, add wallet_id/wallet_name/is_default/deleted_at + the index, remove the wallet_expense_category_mappings CREATE + its indexes. Tests run with foreign_keys = ON, so every expense insert needs a valid wallet or NULL.
</action>
<verify>
<automated>cd packages/db && npm run typecheck</automated>
</verify>
<done>0006 generated SQL inspected and applied locally; 0007 backfill written and journal-registered; DDL constant mirrors schema; packages/db typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: API schemas + services + registration seeding + API tests</name>
  <files>apps/api/src/schemas/expense.ts, apps/api/src/schemas/wallets.ts, apps/api/src/services/expense-service.ts, apps/api/src/services/wallet-service.ts, apps/api/src/services/dashboard-service.ts, apps/api/src/services/auth-service.ts, apps/api/tests/expense.test.ts, apps/api/tests/wallets.test.ts, apps/api/tests/dashboard.test.ts, apps/api/tests/auth.test.ts</files>
  <action>
Implement Steps 4–6 and 10 of the approved plan. Routes (Step 6) need no structural changes — they are schema-driven; just confirm the new 409 surfaces through existing error shaping. Keep routes thin and business logic in services per CLAUDE.md and hono-best-practices.

Schemas:

- expense.ts: delete `PAYMENT_METHOD_VALUES`; in createExpenseSchema remove `paymentMethod`, add `walletId: z.number().int().positive()` (required). updateExpenseSchema stays `.partial()`.
- wallets.ts: delete `expenseModeSchema`; remove `expenseMode` from walletBaseSchema. Keep incomeCategoryIds.

expense-service.ts:

- Interfaces + toResponse: replace paymentMethod with `walletId: number | null` + `walletName: string | null`.
- Add `resolveWalletName(db, walletId, userId)` — query wallets where id + userId + isNull(deletedAt); throw 400 invalid_wallet if missing.
- create: resolve + store walletId/walletName. update: when walletId !== undefined, re-resolve walletName.

wallet-service.ts (the big one):

- Delete setExpenseMappings entirely; remove walletExpenseCategoryMappings / expenseCategories imports.
- getMappingsByWallet: income-only.
- Replace getExpensesByCategoryCents with getExpensesByWalletCents(userId): SELECT wallet_id, SUM(amount) WHERE deleted_at IS NULL AND wallet_id IS NOT NULL GROUP BY wallet_id.
- Balance formula: pfAllocation + mappedIncome − Σ(expenses where walletId = wallet) + deposits − withdrawals.
- assertCanInsertTransaction: delete the WITHDRAWAL branch; keep DEPOSIT guards only; drop expense-mapping args.
- list: drop expense-mapping/autoDeductAllExpenses branches; filter isNull(wallets.deletedAt); drop expenseCategoryIds from response; mappingCount = income-only; include isDefault.
- getById: EXPENSE_AUTO history entries query expenses by walletId = this wallet (drop expenseCatIdsForHistory); mappedExpensesCents from same; still returns detail for soft-deleted wallets so history links keep working.
- create/update: drop expenseMode handling and autoDeductAllExpenses insert.
- remove → SOFT delete: 404 if missing; 409 cannot_delete_default_wallet if isDefault; set deletedAt; hard-delete its walletIncomeCategoryMappings rows; null profitFirstAccountId; keep impact-count response shape.
- Add exported `seedDefaultWallet(db, userId)` helper (mirrors seedProfitFirstAccounts) with Default attributes (name 'Default', isDefault true, color '#10b981', sortOrder 0, profitFirstAccountId null).

dashboard-service.ts: replace expense-mapping query + autoDeductAllExpenses branch in getPeriodScopedWalletBalance with a date-ranged per-wallet expense sum; filter wallets to isNull(deletedAt).

auth-service.ts: call seedDefaultWallet(db, userId) after seedProfitFirstAccounts in register AND in the Google brand-new-user branch.

Tests:

- expense.test.ts: all creates need a seeded wallet + walletId; replace paymentMethod assertions; add — missing walletId → 422; cross-user/soft-deleted wallet → 400; walletName denormalized; legacy NULL-wallet expense still lists.
- wallets.test.ts: remove expenseMode/setExpenseMappings/Pitfall-1/withdrawal-blocked tests; add — balance from expenses.walletId; EXPENSE_AUTO history by walletId; soft delete (sets deletedAt, unlinks income mappings + PF, excluded from list, default → 409); withdrawals allowed everywhere.
- dashboard.test.ts: period-scoped balance via walletId.
- auth.test.ts: Default wallet exists after register (password + Google paths).
- Note: the seedUser test helper bypasses registration — tests needing a Default wallet must seed it explicitly.
  </action>
  <verify>
  <automated>cd apps/api && npx tsc --noEmit && npm run test</automated>
  </verify>
  <done>API typechecks; vitest suite green; register (both paths) seeds Default wallet; soft delete returns 409 for default and unlinks income mappings + PF on others; balances deduct by walletId.</done>
  </task>

<task type="auto">
  <name>Task 3: Web types/constants + expense flows + wallet UI cleanup + repo-wide sweep</name>
  <files>apps/web/src/lib/constants.ts, apps/web/src/types/wallet.ts, apps/web/src/app/(dashboard)/expenses/page.tsx, apps/web/src/components/expense-form.tsx, apps/web/src/app/(dashboard)/expenses/new/_actions/create-expense.ts, apps/web/src/server/expense-actions.ts, apps/web/src/components/edit-expense-dialog.tsx, apps/web/src/components/expense-list.tsx, apps/web/src/components/RecordSheet.tsx, apps/web/src/server/record-sheet.ts, apps/web/src/components/NewWalletForm.tsx, apps/web/src/components/EditWalletDialog.tsx, apps/web/src/app/(dashboard)/wallets/new/page.tsx, apps/web/src/app/(dashboard)/wallets/[walletId]/page.tsx, apps/web/src/components/WalletRow.tsx, apps/web/src/server/wallet-actions.ts</files>
  <action>
Implement Steps 7–9 and 11 of the approved plan. Follow nextjs-best-practices for Server/Client boundaries and CLAUDE.md STRICT structure (server-only modules in src/server/, path aliases @/* and @app/db).

Types & constants (Step 7):

- lib/constants.ts: delete PAYMENT_METHODS + PaymentMethodValue.
- types/wallet.ts: WalletListItem — remove autoDeductAllExpenses/expenseCategoryIds, add isDefault, deletedAt; remove expenseMode from create/update inputs.

Expense flows (Step 8):

- expenses/page.tsx: also fetch /api/wallets; pass wallets {id,name}[] + defaultWalletId down; Expense interface paymentMethod → walletId/walletName.
- expense-form.tsx: replace Payment Method Select with required "Paid with" wallet Select (name="walletId", no "none" option); new props wallets, defaultWalletId; preselect initialValues.walletId ?? defaultWalletId (covers legacy NULL rows).
- expenses/new/\_actions/create-expense.ts + server/expense-actions.ts (updateExpenseAction): read/send walletId (Number, validate positive), drop paymentMethod.
- edit-expense-dialog.tsx: ExpenseRow gains walletId/walletName, loses paymentMethod; thread wallets/defaultWalletId to the form.
- expense-list.tsx: delete paymentMethodLabel; detail line shows walletName (renders even for soft-deleted wallets — denormalized).
- RecordSheet.tsx: "Paid with" select now lists data.wallets, defaults to data.defaultWalletId; send walletId.
- server/record-sheet.ts: RecordSheetData gains wallets + defaultWalletId (fetch /api/wallets, find isDefault); recordExpenseFromSheet input paymentMethod → walletId: number.

Wallet UI cleanup (Step 9):

- NewWalletForm.tsx & EditWalletDialog.tsx: remove the entire "Expense Settings" radio + category-picker blocks, related state (expenseMode, selectedExpenseCategoryIds, expensePickerOpen, otherWalletHasAutoDeductAll), props, and expenseMode in submit payloads. Income-settings UI stays.
- wallets/new/page.tsx & wallets/[walletId]/page.tsx: stop computing/passing mappedExpenseCategoryIds, otherWalletHasAutoDeductAll, expense categories for wallet forms.
- WalletRow.tsx: hide/disable Delete for isDefault wallets; update delete-confirm copy for soft delete ("income mappings and bucket link will be unlinked; past expenses keep this wallet").
- wallet-actions.ts: surface cannot_delete_default_wallet error as a friendly toast.

Repo-wide sweep (Step 11): grep -rn "paymentMethod\|payment_method\|PAYMENT_METHOD\|autoDeductAllExpenses\|auto_deduct_all_expenses\|walletExpenseCategoryMappings\|wallet_expense_category_mappings\|expenseMode\|setExpenseMappings" over apps/ packages/ — must return zero remaining references in shipping code (ignore .wrangler/ artifacts).
</action>
<verify>
<automated>cd apps/web && npx tsc --noEmit && cd /home/orjanbognot/projects/orjan/profitmuna-main && npm run lint && grep -rn "paymentMethod\|payment_method\|PAYMENT_METHOD\|autoDeductAllExpenses\|auto_deduct_all_expenses\|walletExpenseCategoryMappings\|wallet_expense_category_mappings\|expenseMode\|setExpenseMappings" apps/ packages/ --include="_.ts" --include="_.tsx" | grep -v '\.wrangler/' | grep -vc '^$'</automated>
</verify>
<done>Web typechecks; lint passes; "Paid with" is a required wallet selector across expense-form, RecordSheet, and edit dialog defaulting to Default; wallet forms have no Expense Settings; Default wallet delete hidden/blocked; repo-wide sweep returns zero shipping-code references to the removed identifiers.</done>
</task>

</tasks>

<verification>
Full sequence from the approved plan:
```bash
cd packages/db && npx drizzle-kit generate      # inspect 0006 SQL before applying
cd apps/api && npx wrangler d1 migrations apply DB --local
cd packages/db && npm run typecheck
cd apps/api && npx tsc --noEmit && npm run test  # vitest
cd apps/web && npx tsc --noEmit
npm run lint
```

Manual (local dev, seeded UAT user — PBKDF2 passwords, miniflare D1):

1. Register fresh user → "Default" wallet exists, Delete hidden/blocked
2. Add expense via RecordSheet and /expenses/new → "Paid with" lists wallets, defaults to Default; wallet balance drops; can go negative
3. Soft-delete a non-default wallet with income mappings + PF link → gone from /wallets, dashboard, expense picker; name still shows on past expenses; freed income categories/PF account re-linkable
4. Edit a legacy (pre-migration) expense → Default preselected; saving assigns it and it starts deducting
5. Manual withdrawal on a PF-linked wallet → now allowed
   </verification>

<success_criteria>

- payment_method, auto_deduct_all_expenses, wallet_expense_category_mappings, expenseMode, setExpenseMappings fully removed from shipping code (sweep returns 0)
- New/edited expenses require a wallet; balances deduct by walletId and may go negative
- Every user has an undeletable "Default" wallet (register + Google paths)
- Wallets soft-delete; names still render on past expenses; income mappings + PF link freed on delete
- Default wallet delete returns 409; manual withdrawals allowed on all wallets
- All typechecks, lint, and vitest pass
  </success_criteria>

<output>
Create `.planning/quick/260607-l06-move-deduct-from-wallet-onto-expenses-re/260607-l06-SUMMARY.md` when done.
Commit with conventional-commit message per .claude/rules/commits.md, e.g. `feat(expenses): move wallet deduction onto expenses via required Paid-with selector`.
</output>
